package main

/*
= Задача №9. Городской светофор =
Вы делаете симулятор «умного города», и даже светофоры в нём должны быть типобезопасными: не просто числа, а отдельный тип. Для этого вы заводите тип TrafficLight и перечисляете три состояния — красный, жёлтый и зелёный — через iota, чтобы Go сам пронумеровал их по порядку.
Чтобы быстро проверить, что всё действительно нумеруется как задумано, программа печатает значения всех трёх констант в одной строке.
Требования:
• В коде должен быть объявлен собственный тип TrafficLight на базе int (например, type TrafficLight int).
• Должен быть объявлен ровно один const-блок, в котором константы Red, Yellow, Green задаются с использованием iota для автоматической нумерации по порядку.
• Константам Red, Yellow, Green нельзя присваивать значения вручную — в коде не должно быть явных 0, 1, 2 (или других чисел) в их объявлениях.
• Программа должна вывести значения Red, Yellow и Green ровно в одной строке, в виде трёх чисел, разделённых одиночными пробелами.
• Для вывода значений должен использоваться пакет fmt (например, fmt.Print/fmt.Println/fmt.Printf).
• В решении не должны использоваться циклы, а также массивы, слайсы и map.
*/

import "fmt"

type TrafficLight int

// TODO: Объявите собственный тип TrafficLight на базе int (если он ещё не объявлен выше).
// TODO: Объявите ровно один const-блок, в котором константы Red, Yellow, Green задаются через iota.
// TODO: Не присваивайте константам Red, Yellow, Green числовые значения вручную.

const (
    Red TrafficLight = iota
    Yellow
    Green
)

func main() {
	// TODO: Выведите значения Red, Yellow и Green в одной строке: три числа, разделённые одиночными пробелами.
	// TODO: Для вывода используйте пакет fmt.
	fmt.Printf("%d %d %d", Red, Yellow, Green)
}