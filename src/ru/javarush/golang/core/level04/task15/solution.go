package main

/*
= Задача №15. Таблица урона =
Вы балансируете боёвку в мини-RPG: у вас есть множители от 1 до n, и вы хотите распечатать “таблицу урона” — все произведения i*j, чтобы глазами проверить, что формула и порядок вывода корректны.
Программа читает целое число maxMultiplier (1 <= maxMultiplier <= 9) и печатает все произведения i*j для i от 1 до maxMultiplier включительно и для j от 1 до maxMultiplier включительно. Каждая строка должна иметь формат i*j=product, причём сначала выводятся все j для i=1, затем все j для i=2 и так далее.
Требования:
• Программа должна считать одно целое число maxMultiplier из стандартного ввода с помощью fmt.Scan.
• Программа должна использовать два вложенных цикла for (внешний для i и внутренний для j), чтобы перебрать все пары множителей.
• Внешний цикл должен перебирать i от 1 до maxMultiplier включительно, а внутренний — j от 1 до maxMultiplier включительно; вывод должен идти сначала для всех j при i=1, затем для всех j при i=2 и т.д.
• Каждая строка вывода должна иметь строго формат i*j=product, где product — значение произведения i*j.
*/

import "fmt"

func main() {
	// Чтение множителя из stdin через fmt.Scan.
	var maxMultiplier int
	fmt.Scan(&maxMultiplier)

	// TODO: Реализуйте корректные границы циклов:
	// TODO: внешний цикл должен перебирать i от 1 до maxMultiplier включительно,
	// TODO: внутренний цикл должен перебирать j от 1 до maxMultiplier включительно.
	for i := 1; i <= maxMultiplier; i++ {
		for j := 1; j <= maxMultiplier; j++ {
			// TODO: Посчитайте произведение i*j и сохраните в product.
			product := i * j

			// TODO: Выведите строку строго в формате i*j=product (без пробелов), каждая строка с новой строки.
			fmt.Printf("%d*%d=%d\n", i, j, product)
		}
	}
}