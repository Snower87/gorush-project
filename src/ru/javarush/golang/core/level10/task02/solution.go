package main

/*
= Задача №2. Публичное имя =
Вы пишете мини‑проверку для ревью кода: разработчики присылают вам слово (имя функции/типа), а вы должны мгновенно сказать, будет ли это имя “публичным” по правилам Go.
В Go всё решает первая буква: если она заглавная латинская, имя считается экспортируемым.
Программа читает из stdin одно слово name и печатает итог: exported, unexported или invalid, если прочитать имя не удалось (ввод пустой).
Требования:
• Программа должна прочитать из стандартного ввода одно слово `name` с использованием пакета `fmt` (без других способов чтения).
• Если прочитать имя не удалось (ввод пустой), программа должна вывести `invalid`.
• Логика определения “публичности” имени должна быть вынесена в функцию `isExportedName(name string) bool`, которая возвращает `true`, если имя экспортируемое, иначе `false`.
• Функция `isExportedName` должна определять экспортируемость по первому байту строки и считать имя экспортируемым только если первый байт находится в диапазоне латинских заглавных букв `A`–`Z`.
• Программа должна печатать в stdout ровно одно слово без дополнительных сообщений: `exported`, `unexported` или `invalid` (в зависимости от результата).
*/

import "fmt"

// Про экспортируемые идентификаторы пакета:
func isExportedName(name string) bool {
	// TODO: Реализуйте проверку экспортируемости имени по первому байту строки.
	// TODO: Имя считается экспортируемым только если первый байт в диапазоне 'A'–'Z'.
	// TODO: Пустую строку (len == 0) считайте неэкспортируемой.
	if len(name) == 0 {
		return false
	}
    for ch := 'A'; ch <= 'Z'; ch++ {
        if name[0] == ch {
            return true
        }
    }
	return false
}

func main() {
	var name string
	if _, err := fmt.Scan(&name); err != nil {
		fmt.Print("invalid")
		return
	}

	if isExportedName(name) {
		fmt.Print("exported")
	} else {
		fmt.Print("unexported")
	}
}