package main

/*
= Задача №2. Плюс пять =
Вы пишете маленькую утилиту для инженера: в систему приходит измерение (целое число), а инженер просит «прибавь к нему пять и покажи результат». Но вы как разработчик хотите наглядно увидеть разницу между untyped-константой и typed-константой: одна легко подстроится под тип, а другая потребует явного приведения.
Программа читает одно число x и хранит его как int64. После этого она считает две суммы: одну — с untyped-константой add, вторую — с typed-константой addTyped. Затем печатает обе суммы в двух строках.
Требования:
• Программа должна прочитать одно целое число через fmt.Scan и сохранить его в переменной x типа int64.
• Программа должна объявить untyped-константу const add = 5 (без указания типа) и typed-константу const addTyped int = 5.
• Программа должна вычислить sum1 выражением x + add без явного приведения (конвертации) add.
• Программа должна вычислить sum2 выражением x + int64(addTyped), явно приведя typed-константу addTyped к int64.
• Программа должна вывести ровно две строки: сначала untyped=<sum1>, затем typed=<sum2>, без дополнительных строк или текста.
*/

import "fmt"

const add = 5          // TODO: объявите untyped-константу add со значением 5 (без указания типа)
const addTyped int = 5 // TODO: объявите typed-константу addTyped типа int со значением 5

func main() {
	var x int64
	fmt.Scan(&x)

	// TODO: посчитайте сумму x + add без явного приведения add
	sum1 := x + add

	// TODO: посчитайте сумму x + int64(addTyped) с явным приведением typed-константы к int64
	sum2 := x + int64(addTyped)

	fmt.Printf("untyped=%d\n", sum1)
	fmt.Printf("typed=%d\n", sum2)
}