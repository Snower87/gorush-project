package main

/*
= Задача №8. Тихие правки =
Вы обслуживаете табло результатов: сначала приходит стартовый список очков, а потом — пачка «поправок» вида (индекс, новое значение). Ошибочные индексы встречаются, но табло не должно падать с паникой — такие правки просто игнорируются.
Программа читает из stdin число n, затем читает n целых чисел и сохраняет их в слайс scores. После этого читается число m — количество операций обновления. Затем m раз читаются пары idx и value. Для каждой операции обновите scores[idx] = value, только если idx находится в диапазоне от 0 до len(scores)-1. Если индекс вне диапазона — ничего не делайте.
В конце выведите: (1) количество успешно применённых обновлений и (2) итоговый слайс scores.
Требования:
• Программа должна читать из stdin значения n, затем n целых чисел, затем m, затем m пар (idx, value) с использованием fmt.Scan.
• Программа должна создать слайс scores длины n (например, make([]int, n)) и заполнить его исходными числами через индексную запись scores[i] без использования append.
• Для каждой операции (idx, value) программа должна выполнять проверку 0 <= idx && idx < len(scores) и только при корректном индексе делать присваивание scores[idx] = value; при некорректном idx не обращаться к scores[idx] (чтобы не было паники) и просто игнорировать правку.
• Программа должна вести отдельный счётчик успешных обновлений и увеличивать его только для тех операций, которые были применены (индекс был в допустимом диапазоне).
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	// Фиксированная длина: заполняем по индексам, без append.
	scores := make([]int, n)
	for i := 0; i < n; i++ {
		fmt.Scan(&scores[i])
	}

	var m int
	fmt.Scan(&m)

	applied := 0
	for i := 0; i < m; i++ {
		var idx, value int
		fmt.Scan(&idx, &value)

		// TODO: Реализуйте безопасное применение обновления scores[idx] = value:
		// TODO: - сначала проверьте, что 0 <= idx && idx < len(scores)
		// TODO: - только при корректном idx делайте присваивание scores[idx] = value
		// TODO: - увеличивайте applied только для успешно применённых обновлений
		if idx >= 0 && idx < len(scores) {
		    scores[idx] = value
		    applied++
		}
	}

	// Ровно две строки вывода по условию.
	fmt.Println(applied)
	fmt.Println(scores)
}