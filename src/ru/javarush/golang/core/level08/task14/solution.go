package main

/*
= Задача №14. Переключатель флага =
У вас есть «панель переключателей» из 8 бит: текущее состояние хранится в числе flags. Пользователь просит включить бит k, но вам важно сначала сообщить, был ли этот переключатель уже включён до изменения — чтобы понимать, было ли это повторное действие.
Программа читает flags и k, печатает в первой строке present или absent, а во второй — новое значение после включения бита и его двоичное представление шириной 8.
Требования:
• Программа должна прочитать два числа `flags` и `k` через `fmt.Scan`.
• Переменные `flags` и `mask` должны иметь тип `uint`, а маска нужного бита должна вычисляться как `mask = 1 << k`.
• Программа должна определить, был ли бит `k` установлен до изменения, используя проверку `(flags & mask) != 0`.
• Программа должна получить новое значение `newFlags`, включив бит `k` операцией `newFlags = flags | mask` (то есть после выполнения бит `k` обязан быть установлен).
• Программа должна вывести ровно две строки: в первой — ровно одно слово `present` (если бит был установлен) или `absent` (если не был), во второй — новое значение `newFlags` и его двоичное представление шириной 8 через формат `%08b`.
*/

import "fmt"

func main() {
	var flags, k uint
	fmt.Scan(&flags, &k)

	var mask uint
	mask = 1 << k
	// TODO: Вычислите маску для бита k с помощью битового сдвига (нужен беззнаковый тип)

	// TODO: Определите, был ли бит k установлен ДО изменения (проверка через побитовое И),
	// TODO: и выведите "present" или "absent" ровно в одной строке.
	if mask & flags != 0 {
	    fmt.Println("present")
	} else {
	    fmt.Println("absent")
	}

	newFlags := flags | mask
	// TODO: Сформируйте новое значение флагов так, чтобы бит k был включён (операция побитового ИЛИ)

	fmt.Printf("%d %08b\n", newFlags, newFlags)
}