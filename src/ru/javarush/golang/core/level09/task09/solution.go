package main

/*
= Задача №9. Два ориентира =
Вы пишете модуль для навигатора робота: он получает две отметки высоты (в целых единицах) и должен быстро понять нижнюю и верхнюю границу диапазона. Для читаемости вы хотите, чтобы сама сигнатура функции «документировала», что возвращается минимум и максимум.
Программа читает два целых числа heightA и heightB и выводит сначала минимум, затем максимум через пробел. Вынесите выбор в функцию minMax(a, b int), где результаты названы прямо в сигнатуре как (min int, max int). Внутри присваивайте значения в min и max, а возвращайтесь через return без значений (naked return).
Пример ввода:
10 3
Пример вывода:
3 10
Требования:
• Программа должна содержать функцию minMax(a, b int) с сигнатурой возвращаемых значений (min int, max int).
• Внутри minMax значения должны присваиваться именно в переменные min и max, причём перед каждым выходом из функции min и max должны быть присвоены явно (включая ветку a == b).
• Функция minMax должна завершаться оператором return без указания возвращаемых значений (использовать naked return).
• Функция main должна прочитать ровно два целых числа heightA и heightB с помощью fmt.Scan.
• Программа должна вывести одну строку в формате “min max” (минимум, затем максимум через пробел); при a == b вывод должен быть “a a”.
*/

import "fmt"

// minMax возвращает минимум и максимум; результаты названы в сигнатуре.
func minMax(a, b int) (min int, max int) {
	// TODO: Реализуйте корректное определение минимума и максимума для a и b.
	// TODO: Убедитесь, что перед выходом из функции min и max присвоены явно (включая случай a == b).

	if a <= b {
		min = a
		max = b
	} else {
		// Сейчас ветка работает неправильно для a > b.
		min = b
		max = a
	}

	// Naked return по требованию
	return
}

func main() {
	var heightA, heightB int
	fmt.Scan(&heightA, &heightB)

	min, max := minMax(heightA, heightB)
	fmt.Printf("%d %d", min, max)
}