package main

/*
= Задача №13. Порядок запуска =
Вы объясняете коллеге, почему иногда “что-то печатается ещё до main”. Чтобы показать это максимально наглядно, вы делаете крошечную программу‑демонстрацию: одна строка выводится из init(), а следующая — из main().
Никакого ввода, никаких дополнительных сообщений — только чистый порядок запуска.
Ожидаемый вывод:
 init
 main
Требования:
• Программа должна быть в пакете main.
• В коде должны быть определены ровно две функции: init() и main(), без дополнительных функций.
• Функции init() и main() не должны принимать параметров и не должны возвращать значения.
• Для вывода строк должен использоваться пакет fmt.
• Программа должна вывести ровно две строки, строго в порядке: первая строка "init", вторая строка "main", без дополнительных сообщений.
*/

import "fmt"

func init() {
	// TODO: Выведите первую строку в init() строго по условию задачи (без лишних сообщений).
	fmt.Println("init")
}

func main() {
	// TODO: Выведите вторую строку в main() строго по условию задачи (без лишних сообщений).
	fmt.Println("main")
}