package main

/*
= Задача №13. Паспортное приветствие =
Вы пишете консольный “мини-регистратор” для мероприятия: человек вводит имя (одно слово) и возраст (целое число), а вы печатаете аккуратное приветствие по шаблону, которое сразу можно показать на бейдже.
Программа должна считать имя и возраст, а затем вывести ровно одну строку строго в формате: Hello, <name>! Age=<age> В конце строки должен быть перевод строки.
Печатать нужно через fmt.Printf, используя %s для имени и %d для возраста — как в настоящем шаблоне отчёта.
Требования:
• Программа должна считать из stdin два значения: имя (одно слово) и возраст (целое число) с помощью fmt.Scan или fmt.Fscan.
• Имя должно храниться в переменной типа string, возраст — в переменной типа int.
• Программа должна вывести результат ровно одной строкой, используя fmt.Printf.
• В выводе должны использоваться спецификаторы %s для имени и %d для возраста, а количество спецификаторов должно совпадать с количеством переданных аргументов.
*/

import "fmt"

func main() {
	var name string
	var age int

	fmt.Scan(&name, &age)

	// TODO: Выведите приветствие строго в формате из условия, используя fmt.Printf, %s (для имени) и %d (для возраста), и перевод строки в конце.
	// Важно: вывод должен быть ровно одной строкой, без лишних пробелов и дополнительных строк.
	fmt.Printf("Hello, %s! Age=%d\n", name, age)
}