package main

/*
= Задача №4. Внутренний отчёт =
Вы делаете консольный "финансовый чекер": на вход приходят три числа — income, expense и tax — и нужно вывести аккуратную строку отчёта, где сразу видно и входные данные, и итоговый баланс balance = income - expense - tax.
Чтобы код было легко поддерживать, команда хочет мини‑API прямо внутри package main: одна “публичная” функция собирает итоговую строку, а внутри неё работают маленькие помощники для вычисления и форматирования.
Требования:
• Проект должен состоять ровно из двух файлов: main.go и report.go; оба файла должны объявлять package main.
• Программа должна прочитать из stdin три целых числа в порядке: income, expense, tax.
• В report.go должна быть объявлена exported-функция PrintBalanceReport, которая возвращает string и формирует итоговую строку отчёта.
• В report.go должны быть реализованы как минимум две unexported-функции (имя с маленькой буквы), которые используются внутри PrintBalanceReport для вычислений и/или форматирования.
• Формула balance = income - expense - tax не должна присутствовать в main.go; вычисление баланса должно происходить внутри функций из report.go.
*/

import "fmt"

func main() {
	var income, expense, tax int
	fmt.Scan(&income, &expense, &tax)

	// Печатаем готовую строку отчёта из "мини-API" (package main).
	fmt.Printf("%s\n", PrintBalanceReport(income, expense, tax))
}