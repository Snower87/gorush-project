package main

/*
= Задача №1. Паспорт констант =
Вы настраиваете «панель диагностики» для маленького Go‑сервиса. В конфиге сервиса лежат разные константы: числа, текст, логический флаг и даже символ. Вам нужно вывести их так, чтобы по распечатке было видно не только значение, но и то, какой тип Go на самом деле за ними стоит (особенно интересно посмотреть на символ).
Программа не читает никакой ввод. Она просто объявляет набор констант и печатает по одной строке на каждую: имя константы, её значение и её тип — как в мини‑паспорте.
Требования:
• Программа не должна читать данные из stdin и не должна ожидать пользовательский ввод; вся работа выполняется за счёт объявленных в коде констант.
• Код должен находиться в пакете main и содержать функцию main, которая запускает вывод “паспортов” констант.
• Должны быть объявлены untyped-константы без явного указания типа со значениями: n = 5, f = 1.25, s = "go", b = true, r = 'A'; при этом в решении должны использоваться и одиночный const, и const-блок вида const ( ... ).
• Должны быть объявлены typed-константы: typedN типа int со значением 5 и typedS типа string со значением "go".
• Вывод должен содержать ровно 7 строк, по одной строке на каждую из констант (n, f, s, b, r, typedN, typedS), и каждая строка должна иметь формат: имя=<значение> type=<тип>.
*/

import "fmt"

// В этой задаче важно увидеть разницу между typed и untyped константами
// через печать значения и реального типа.

// Одиночный const (по требованиям)
const n = 5

// const-блок (по требованиям)
const (
	f = 1.25
	s = "go"
	b = true
	r = 'A'
)

// typed-константы с явным типом (по требованиям)
const (
	typedN int    = 5
	typedS string = "go"
)

func main() {
	// TODO: Выведите ровно 7 строк — по одной на каждую константу: n, f, s, b, r, typedN, typedS.
	// TODO: Формат каждой строки: имя=<значение> type=<тип>.
	// TODO: Для печати используйте fmt.Printf так, чтобы значение выводилось через %v, а тип — через %T.
	// TODO: Обратите внимание на r: в выводе значение должно получиться числом, а тип — int32.

	// Сейчас вывод намеренно неполный и в неверном формате.
	fmt.Printf("n=%v type=%T\n", n, n)
	fmt.Printf("f=%v type=%T\n", f, f)
	fmt.Printf("s=%v type=%T\n", s, s)
	fmt.Printf("b=%v type=%T\n", b, b)
	fmt.Printf("r=%v type=%T\n", r, r)
	fmt.Printf("typedN=%v type=%T\n", typedN, typedN)
	fmt.Printf("typedS=%v type=%T\n", typedS, typedS)
}