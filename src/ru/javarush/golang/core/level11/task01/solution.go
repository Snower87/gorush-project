package main

/*
= Задача №1. Двойной чемодан =
Вы на стойке регистрации в аэропорту и заполняете карточку багажа: у вас есть один чемодан с тремя вещами, и вы решили сделать «точную копию списка», чтобы один экземпляр отдать в службу розыска, а второй — оставить себе.
Программа читает из stdin три целых числа и складывает их в массив a типа [3]int. Затем вы создаёте второй массив b простым присваиванием b := a, как будто делаете копию карточки. После этого вы меняете только копию: увеличиваете первый элемент массива b в 10 раз. Оригинальный массив a при этом должен остаться прежним.
В конце вы печатаете оба массива в двух строках, чтобы диспетчер сразу видел, что изменялась только копия.
Требования:
• Программа должна хранить введённые значения в массиве a типа [3]int (использование []int не допускается).
• Программа должна прочитать из stdin ровно три целых числа с помощью fmt.Scan и записать их в a[0], a[1], a[2].
• Программа должна создать второй массив b простым присваиванием b := a (без циклов копирования и без функций copy).
• Программа должна изменить только первый элемент массива b, увеличив b[0] в 10 раз; массив a при этом должен остаться без изменений.
• Программа должна вывести результат ровно в двух строках: одна строка для массива a и одна строка для массива b, без дополнительных строк.
*/

import "fmt"

func main() {
	var a [3]int
	fmt.Scan(&a[0], &a[1], &a[2])

	//var b [3]int
	// TODO: Создайте массив b как копию массива a с помощью простого присваивания (без циклов и без copy).
	// TODO: Измените только массив b: увеличьте b[0] в 10 раз так, чтобы массив a не изменился.
	b := a
	b[0] = b[0] * 10

	fmt.Println("a:", a)
	fmt.Println("b:", b)
}