package main

/*
= Задача №2. Индекс сторож =
Представьте, что у вас есть строка inputToken — это «код предмета» в игре, и вам нужно достать байт по позиции indexToCheck. Но игроки вводят что угодно, поэтому программа должна быть осторожной: никакого panic, только проверка границ.
Программа читает строку s (без пробелов) и целое число i. Если индекс попадает в диапазон [0, len(s)), выведите одну строку byte=<N> char=<C>, где <N> — десятичное значение байта s[i], а <C> — тот же байт, напечатанный как символ.
Если индекс неправильный (меньше 0 или не меньше len(s)), выведите ровно OUT_OF_RANGE.
Требования:
• Программа должна прочитать ровно два значения из stdin: строку s (без пробелов) и целое число i.
• Перед обращением к s[i] программа должна проверить, что i находится в диапазоне [0, len(s)), чтобы исключить panic “index out of range”.
• Проверка границ должна быть выполнена через сравнения i с 0 и len(s) (i < 0 или i >= len(s)).
• Если индекс корректный, программа должна получить байт строго операцией s[i] (индексирование строки).
• При корректном индексе программа должна вывести ровно одну строку в формате byte=<N> char=<C>, где <N> — десятичное значение байта s[i], а <C> выводится через форматирование %c.
*/

import "fmt"

func main() {
	var s string
	var i int
	fmt.Scan(&s, &i)

	// TODO: Реализуйте проверку, что i находится в диапазоне [0, len(s)) ДО обращения к s[i], чтобы избежать panic.
	// TODO: Проверка границ должна использовать сравнения только с 0 и len(s) (i < 0 или i >= len(s)).
	// TODO: Если индекс некорректный — выведите ровно OUT_OF_RANGE без лишних символов и завершите программу.
	// TODO: Если индекс корректный — получите байт строго через s[i] и выведите ровно: byte=<N> char=<C> (для символа используйте %c).
	if i < 0 || i >= len(s) {
	    fmt.Print("OUT_OF_RANGE")
	    return
	}
    fmt.Printf("byte=%d char=%c", s[i], s[i])
}