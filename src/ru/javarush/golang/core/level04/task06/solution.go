package main

/*
= Задача №6. Контрольный код посылки =
На складе у каждой посылки есть числовой код. Чтобы проверить, что код не “битый”, вы хотите быстро посчитать сумму его цифр: это простая, но полезная проверка в реальных системах (и отличная тренировка циклов).
Программа читает неотрицательное целое число packageCode и вычисляет сумму его десятичных цифр, используя цикл for cond { ... }. Цикл должен выполняться, пока packageCode > 0. На каждой итерации берётся последняя цифра через packageCode % 10, добавляется к сумме digitsSum, после чего число уменьшается делением packageCode /= 10. Если на вход подано 0, программа должна вывести 0.
Требования:
• Программа должна прочитать из stdin одно неотрицательное целое число packageCode (packageCode >= 0).
• Программа должна вычислять сумму десятичных цифр числа, используя цикл вида for packageCode > 0 { ... }, который выполняется, пока packageCode > 0.
• На каждой итерации цикла программа должна брать последнюю цифру через packageCode % 10, добавлять её к переменной суммы (например, digitsSum), после чего уменьшать packageCode операцией packageCode /= 10.
• Сумма цифр должна накапливаться в отдельной переменной типа int (например, digitsSum), а не вычисляться “на лету” без хранения результата.
• Если на вход подано 0, программа должна вывести 0; в остальных случаях вывести одно целое число — сумму цифр (итоговое значение digitsSum) в stdout.
*/

import "fmt"

func main() {
	var packageCode int
	fmt.Scan(&packageCode)

	digitsSum := 0

	for packageCode > 0 {
		// TODO: Реализуйте вычисление суммы цифр числа.
		// TODO: На каждой итерации нужно получить последнюю цифру (packageCode % 10) и добавить её в digitsSum.
		digitsSum += packageCode % 10
		// TODO: Затем нужно уменьшить packageCode (packageCode /= 10), чтобы перейти к следующей цифре.
		packageCode /= 10
		// TODO: Уберите break после того, как реализуете корректное обновление packageCode, иначе цикл выполнится только один раз.
		//break
	}

	fmt.Print(digitsSum)
}