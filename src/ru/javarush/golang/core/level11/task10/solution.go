package main

/*
= Задача №10. Режим пустоты =
Вы пишете утилиту для отладки: оператор вводит слово-команду, и программа создаёт слайс в одном из «режимов пустоты», чтобы потом можно было посмотреть его свойства (nil, len, cap) и понять, что именно получилось.
Программа читает из stdin одно слово mode. Возможные значения: "nil", "empty", "cap". В зависимости от mode создайте слайс s типа []int: для "nil" — nil-слайс, для "empty" — пустой, но не nil, а для "cap" дополнительно прочитайте целое число k и создайте пустой слайс с ёмкостью k.
После этого выведите одну строку: "nil=<true/false> len=<число> cap=<число>".
Требования:
• Программа должна читать из stdin слово mode с помощью fmt.Scan; при mode=cap дополнительно читать одно целое число k тем же способом.
• Для режимов nil и empty программа должна читать ровно одно значение (mode); для режима cap — ровно два значения (mode и k).
• Программа должна создать слайс s типа []int так, чтобы при mode=nil получался nil-слайс, при mode=empty — пустой, но не nil, а при mode=cap — пустой слайс с ёмкостью k.
• Для mode=empty должен использоваться пустой литерал []int{}; для mode=cap должен использоваться make([]int, 0, k); для mode=nil должен использоваться var s []int (или эквивалентный способ получить nil-слайс без make).
• Программа должна вывести ровно одну строку формата `nil=<true/false> len=<число> cap=<число>`, где значения соответствуют (s == nil), len(s) и cap(s).
*/

import "fmt"

func main() {
	var mode string
	fmt.Scan(&mode)

	// s должен быть []int в одном из трёх "режимов пустоты"
	var s []int // задали просто шаблон - тут мы пока его не как не определили

	switch mode {
	case "nil":
		// TODO: Реализуйте создание nil-слайса (в этом режиме s должен остаться nil).
	case "empty":
		// TODO: Реализуйте создание пустого, но НЕ nil, слайса.
		// Намеренно оставлено некорректное поведение для шаблона.
		s = []int{}
	case "cap":
		var k int
		fmt.Scan(&k)

		// TODO: Реализуйте создание пустого слайса с ёмкостью k.
		// Намеренно оставлено некорректное поведение для шаблона.
		s = make([]int, 0, k)
	}

	fmt.Printf("nil=%v len=%d cap=%d\n", s == nil, len(s), cap(s))
}