package main

/*
= Задача №15. Явная инициализация =
Вы пишете маленькое CLI‑приложение, которое должно стартовать с "тегом" — например, shop: или bot: — и печатать готовность в виде строки <tag>: ready. Но в этот раз вы принципиально не используете init(): инициализация должна быть явной, через отдельную функцию, которая может вернуть ошибку.
Программа читает одно слово appTag, вызывает initApp(appTag string) (string, error). Если тег пустой — возвращается ошибка "empty app tag". В main при ошибке нужно вывести init error: <текст ошибки> и завершиться, а при успехе вывести <результат>ready.
Требования:
• В программе не должна быть объявлена функция init() (никаких неявных инициализаций через init).
• Программа должна прочитать ровно одно слово appTag из стандартного ввода (без чтения дополнительных значений).
• В программе должна быть реализована функция initApp(appTag string) (string, error), которую вызывает main для инициализации на основе введённого тега.
• Если appTag пустой, initApp должна вернуть ошибку с текстом "empty app tag", созданную через errors.New или fmt.Errorf (сообщение — строчными буквами, без точки в конце).
• main() должна обрабатывать ошибку через шаблон if err != nil { … return } (без использования panic), печатая строку вида "init error: <текст ошибки>" и завершая выполнение.
*/

import (
	"fmt"
	"os"
	"errors"
)

// init() намеренно не используем: инициализация должна быть явной.

func initApp(appTag string) (string, error) {
	// TODO: Реализуйте явную инициализацию приложения.
	// TODO: Проверьте корректность appTag (пустая строка должна приводить к ошибке с нужным текстом).
	// TODO: При успехе верните строку-префикс, из которой main сможет собрать итоговый вывод.
	if appTag == "" {
	    return "", errors.New("empty app tag")
	}
	return appTag, nil
}

func main() {
	var appTag string
	fmt.Fscan(os.Stdin, &appTag) // читаем ровно одно слово

	prefix, err := initApp(appTag)
	if err != nil {
		fmt.Print("init error: ", err)
		return
	}

	fmt.Print(prefix, "ready")
}