package main

/*
= Задача №10. Угловая рамка =
Вы делаете отладочный вывод для парсера: хотите печатать полученный токен так, чтобы он был явно «обрамлён» и не сливался с соседним текстом. Договорились, что любой токен выводится как <token>.
Программа читает из stdin одну строку (один токен) и должна вывести новую строку вида <ввод>.
Собирать результат нужно не конкатенацией строк, а через []byte: создать слайс, добавить байт '<', затем байты исходной строки (через раскрытие s...), затем байт '>', и только в конце превратить всё в string.
Требования:
• Программа должна прочитать из stdin одну строку (один токен без пробелов) и сохранить её в переменную типа string.
• Программа должна вывести строку строго в формате "<s>", где s — считанная строка.
• Результирующая строка должна собираться в слайсе байт: добавить байт '<', затем содержимое исходной строки, затем байт '>' — все операции добавления выполняются через append.
• Байты исходной строки должны добавляться в []byte именно через append(b, s...) (раскрытие строки как последовательности байт), без промежуточных преобразований в []rune и без посимвольных циклов как замены требованию.
• Готовый результат должен быть превращён в строку только один раз — через string(b) — и уже эта строка выводится.
*/

import (
	"fmt"
	"os"
)

func main() {
	var s string
	fmt.Fscan(os.Stdin, &s)

	b := make([]byte, 0, len(s)+2)

	// TODO: Соберите результат строго в формате "<s>" через []byte и append:
	// TODO: 1) добавьте байт '<'
	// TODO: 2) добавьте байты строки s через append с раскрытием s...
	// TODO: 3) добавьте байт '>'
	// TODO: После этого один раз преобразуйте b в string и выведите.
	b = append(b, '<')
	b = append(b, s...)
	b = append(b, '>')

	fmt.Println(string(b))
}