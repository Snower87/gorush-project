package main

/*
= Задача №17. Квадрат заряда =
Вы пишете диагностику для маленького "энергоблока": оператор вводит целое число — уровень заряда. Система должна выдать
“квадрат” этого значения (так устроена внутренняя формула). Но есть проблема: иногда в терминал прилетает мусор, и тогда
нельзя продолжать расчёты — нужно честно сообщить об ошибке и остановиться.
Программа читает из stdin одно целое число chargeLevel с помощью fmt.Scan.
Если чтение не удалось (fmt.Scan вернул err != nil), программа должна вывести строку: ошибка ввода и завершиться.
Если чтение успешно, программа должна вычислить chargeLevel*chargeLevel и вывести результат как одно целое число без дополнительного текста.
Требования:
• Код должен быть оформлен как `package main` и содержать функцию `func main()`.
• Программа должна прочитать из `stdin` ровно одно целое число `chargeLevel` с помощью `fmt.Scan`.
• Сразу после вызова `fmt.Scan` программа должна проверить ошибку через `if err != nil`.
• Если `err != nil`, программа должна вывести ровно строку `ошибка ввода` (без лишних пробелов/дополнительного текста) и перейти к завершению.
• После вывода `ошибка ввода` программа не должна продолжать обычный сценарий вычислений (должен быть ранний выход, например `return` из `main`).
*/

import "fmt"

func main() {
	var chargeLevel int

	// Читаем одно целое число.
	_, err := fmt.Scan(&chargeLevel)
	if err != nil {
		fmt.Print("ошибка ввода")
		return
	}

	result := chargeLevel * chargeLevel
	// TODO: Вычислите квадрат значения chargeLevel и сохраните в result.

	fmt.Print(result)
}