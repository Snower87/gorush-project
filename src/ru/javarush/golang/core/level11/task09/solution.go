package main

/*
= Задача №9. Три профиля =
Вы настраиваете три «профиля хранения» для одного и того же типа данных: один профиль вообще пустой и не выделял память, второй — пустой, но уже настоящий слайс, а третий — пустой, но с заранее подготовленным запасом ёмкости под будущие элементы.
Создайте три переменные типа []int с именами a, b, c так, чтобы a был nil-слайсом, b был пустым (empty) слайсом, но не nil, а c был пустым (empty) слайсом, но не nil, и при этом имел cap равный 3.
Затем выведите ровно три строки — по одной на каждый слайс — в формате <имя>: nil=<true/false> len=<число> cap=<число>.
Требования:
• В программе должна быть объявлена переменная `a` типа `[]int` через `var a []int`, так чтобы `a` был nil-слайсом.
• В программе должна быть создана переменная `b` типа `[]int` как пустой литерал слайса (например, `b := []int{}`), так чтобы `b` был empty-слайсом, но не nil.
• В программе должна быть создана переменная `c` типа `[]int` через `make` с длиной `0` и ёмкостью `3` (например, `c := make([]int, 0, 3)`), так чтобы `c` был empty-слайсом, не nil, и `cap(c)` был равен 3.
• Программа должна вывести ровно три строки (по одной для `a`, `b`, `c`) в точном формате `<имя>: nil=<true/false> len=<число> cap=<число>`, включая пробелы и порядок полей.
• Для определения `nil`, `len` и `cap` в выводе должны использоваться только выражения `x == nil`, `len(x)`, `cap(x)` отдельно для каждого из слайсов `a`, `b`, `c`.
*/

import "fmt"

func main() {
	// a — nil-слайс (объявлен через var, памяти под элементы нет)
	var a []int

	// TODO: Создайте переменную b типа []int как пустой (empty) слайс через литерал, но НЕ nil.
	var b []int{}

	// TODO: Создайте переменную c типа []int как пустой (empty) слайс, но НЕ nil, с cap равным 3.
	c := make([]int, 0, 3)

	fmt.Printf("a: nil=%t len=%d cap=%d\n", a == nil, len(a), cap(a))
	fmt.Printf("b: nil=%t len=%d cap=%d\n", b == nil, len(b), cap(b))
	fmt.Printf("c: nil=%t len=%d cap=%d\n", c == nil, len(c), cap(c))
}