package main

/*
= Задача №15. Паспорт строки =
Вы отлаживаете ввод "кода детали": это строка без пробелов. Чтобы быстро понять, что именно пришло, вам нужно распечатать “паспорт” строки: для каждого индекса показать символ, который там стоит. Но по условиям стенда разрешён только доступ по индексам S[i] и границы через len(S).
Программа читает строку partCode и печатает len(partCode) строк. В каждой строке выводится i:c, где i — индекс от 0, а c — символ по этому индексу. Гарантия: строка состоит только из ASCII, значит можно брать символ как partCode[i]. Если строка пустая, вывода не должно быть вообще.
Требования:
• Программа должна прочитать строку partCode из stdin с помощью fmt.Scan.
• Программа должна использовать классический цикл for с индексом i, начиная с 0, с условием цикла строго вида i < len(partCode) и инкрементом i++.
• Внутри цикла программа должна получать символ только индексированием partCode[i] (без range по строке) и не выполнять обращение к partCode[len(partCode)].
• Программа должна вывести ровно len(partCode) строк, по одной на каждый индекс i, в формате i:c (индекс, двоеточие, символ), по одной паре на строку.
• Если partCode пустая (len(partCode) == 0), программа не должна печатать ничего (0 строк).
*/

import "fmt"

func main() {
	var partCode string
	fmt.Scan(&partCode)

	// TODO: Напечатайте “паспорт” строки partCode: для каждого индекса i (от 0 до len(partCode)-1)
	// TODO: выведите строку в формате i:c (индекс, двоеточие, символ) на отдельной строке.
	// TODO: Используйте классический цикл for с условием строго вида i < len(partCode) и инкрементом i++.
	// TODO: Внутри цикла получайте символ только через partCode[i] и не печатайте ничего для пустой строки.

	for i := 0; i < len(partCode); i++ {
	    fmt.Printf("%d:%v\n", i, partCode[i])
	}
}