package main

/*
= Задача №19. Деление без риска =
Вы пишете консольный “мини-делитель” для инженеров. Но входные данные приходят как две строки (например, из копипаста). Вам нужно строго и по шагам:
- убедиться, что вообще прочитали два значения,
- отдельно распарсить каждое,
- отдельно проверить деление на ноль,
- и только потом — делить.
Программа читает из stdin два значения: aStr и bStr (две строки) одной операцией fmt.Scan.
Если fmt.Scan не смог прочитать два значения (err != nil), программа должна вывести: ошибка ввода и завершиться.
Затем программа должна преобразовать aStr и bStr в int через strconv.Atoi, отдельно проверяя ошибки. Если aStr не парсится, вывод: ошибка первого числа Если bStr не парсится, вывод: ошибка второго числа В обоих случаях программа завершает выполнение.
Если оба числа успешно получены, но b равно 0, программа должна вывести: деление на ноль и завершиться.
Иначе программа должна вывести целочисленный результат a / b.
Требования:
• Программа должна прочитать из stdin ровно два значения одной операцией fmt.Scan в переменные aStr и bStr (без дополнительных попыток ввода и без циклов).
• Сразу после fmt.Scan программа должна проверить err; если err != nil, вывести точное сообщение "ошибка ввода" и завершить выполнение.
• Программа должна преобразовать aStr в int через strconv.Atoi и сразу после вызова отдельно проверить err; при ошибке вывести "ошибка первого числа" и завершить выполнение.
• Программа должна преобразовать bStr в int через strconv.Atoi и сразу после вызова отдельно проверить err; при ошибке вывести "ошибка второго числа" и завершить выполнение.
• После успешного парсинга обоих чисел программа должна отдельно проверить условие b == 0 (не как ошибку Atoi); если b == 0, вывести "деление на ноль" и завершить выполнение.
*/

import (
	"fmt"
	"strconv"
)

func main() {
	var aStr, bStr string
	_, err := fmt.Scan(&aStr, &bStr)
	if err != nil {
		// TODO: Выведите "ошибка ввода" и завершите программу.
		fmt.Print("ошибка ввода")
		return
	}

	// Внимание: здесь намеренно сделана неполная/неправильная обработка ошибок.
	// TODO: Сделайте независимую проверку ошибки парсинга первого числа сразу после strconv.Atoi(aStr).
	a, err := strconv.Atoi(aStr)
	if err != nil {
	    fmt.Println("ошибка первого числа")
	    return
	}

	b, err := strconv.Atoi(bStr)
	if err != nil {
		// TODO: Сделайте независимую проверку ошибки парсинга второго числа сразу после strconv.Atoi(bStr).
		// TODO: При ошибках парсинга выводите точные сообщения и завершайте программу.
		fmt.Print("ошибка второго числа")
		return
	}

	if b == 0 {
		// TODO: Выведите "деление на ноль" и завершите программу (не выполняйте деление).
		fmt.Print("деление на ноль")
		return
	}

	// TODO: Убедитесь, что при любой ошибке деление не выполняется и нет лишнего вывода.
	fmt.Print(a / b)
}