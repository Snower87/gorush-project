package main

/*
= Задача №17. Итог смены =
Вы закрываете смену в маленьком кафе. Бариста вводит, сколько заказов было (n), а затем по очереди вводит итог по каждому заказу (целыми числами). Нужно получить общий итог смены — быстро и без хранения всего списка заказов.
Программа читает целое число ordersCount, затем читает ordersCount целых чисел и вычисляет их сумму. Вход подаётся в два этапа: сначала ordersCount, затем последовательность из ordersCount чисел. Гарантируется, что все числа помещаются в тип int. Программа должна вывести одно целое число — сумму всех введённых значений. Если ordersCount равно 0, программа должна вывести 0.
Требования:
• Программа должна считать целое число ordersCount с помощью fmt.Scan как количество последующих значений.
• Программа должна считать ровно ordersCount целых чисел (каждое через fmt.Scan) в цикле for со счётчиком.
• Программа должна накапливать общую сумму в переменной-аккумуляторе (например, totalRevenue), увеличивая её на каждое считанное значение; использование слайсов, массивов и map запрещено.
• Переменная суммы должна быть инициализирована значением 0 до начала чтения сумм заказов.
• Если ordersCount равно 0, программа не должна пытаться читать значения заказов и должна вывести 0.
*/

import "fmt"

func main() {
	var ordersCount int
	fmt.Scan(&ordersCount)

	totalRevenue := 0

	for i := 0; i < ordersCount; i++ {
		var orderTotal int
		fmt.Scan(&orderTotal)

		// TODO: Реализуйте накопление суммы всех заказов в переменной totalRevenue
		// Сейчас логика неверная: сохраняется только значение текущего заказа.
		totalRevenue += orderTotal
	}

	fmt.Println(totalRevenue)
}