package main

/*
= Задача №15. Контроль переполнения =
Вы пишете "шлюз" между системами: старая часть приложения принимает значения как int8 (очень маленький диапазон), а новая часть присылает их как int64. Вам нельзя просто привести тип — иначе при выходе за диапазон получится мусор. Нужна явная проверка границ.
Программа читает одно целое число incomingValue (тип int64). Если оно лежит в диапазоне int8 от -128 до 127 включительно, программа преобразует его в int8 и выводит число. Если не помещается — печатает "OVERFLOW".
Требования:
• Программа должна считать одно целое число через fmt.Scan в переменную incomingValue типа int64.
• Программа должна проверить, что incomingValue находится в диапазоне от -128 до 127 включительно, используя явно заданные числовые границы -128 и 127.
• Программа должна использовать конструкцию if/else для выбора между успешным преобразованием и обработкой переполнения.
• Преобразование incomingValue к типу int8 должно выполняться только в ветке, где значение прошло проверку диапазона.
*/

import "fmt"

func main() {
	var incomingValue int64
	fmt.Scan(&incomingValue)

	// TODO: Реализуйте явную проверку, помещается ли incomingValue в диапазон int8.
	// TODO: Используйте if/else: в ветке успеха — безопасно преобразуйте к int8 и выведите число,
	// TODO: иначе — выведите строку OVERFLOW.
	fitsInt8 := (incomingValue >= -128) && (incomingValue <= 127)

	if fitsInt8 {
		// TODO: Выполните преобразование к int8 только после успешной проверки диапазона.
		fmt.Println(int8(incomingValue)
	} else {
		fmt.Println("OVERFLOW")
	}
}