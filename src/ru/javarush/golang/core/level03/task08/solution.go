package main

/*
= Задача №8. Возраст из анкеты =
Вы получаете анкету пользователя, но возраст приходит строкой: кто-то вводит "21", а кто-то — "двадцать один".
Вам нужно аккуратно попробовать распарсить число и в случае ошибки честно сообщить, что данные невалидны.
Программа читает одну строку ageStr (одно “слово” без пробелов). Затем пытается преобразовать её в целое число через
strconv.Atoi, используя if с инициализатором вида: if age, err := ...; err != nil { ... } else { ... }
Правила вывода:
- Если строка не преобразуется в число, вывести INVALID.
- Если число получилось, но оно меньше 0, вывести INVALID.
- Иначе вывести категорию: FREE (age < 7), CHILD (age < 18), ADULT (иначе).
Требования:
• Программа должна прочитать одну строку ageStr через fmt.Scan (одно слово без пробелов).
• Программа должна пытаться распарсить возраст с помощью конструкции вида: if age, err := strconv.Atoi(ageStr); err != nil { ... } else { ... }.
• Если парсинг завершился ошибкой (err != nil) или если распарсенное число age < 0, программа должна вывести INVALID.
• Если данные валидны, программа должна определить категорию по возрасту в порядке сверху вниз: FREE при age < 7, иначе CHILD при age < 18, иначе ADULT, используя цепочку else if.
• Переменные age и err должны быть объявлены внутри if с инициализатором и не должны использоваться вне соответствующего блока if/else.
*/

import (
	"fmt"
	"strconv"
)

func main() {
	var ageStr string
	fmt.Scan(&ageStr)

	// Парсим возраст строго через if с инициализатором: age и err живут только внутри этого if/else.
	if age, err := strconv.Atoi(ageStr); err != nil {
		fmt.Print("INVALID")
	} else {
		// После успешного парсинга отдельно проверяем невалидный диапазон.
		if age < 0 {
			fmt.Print("INVALID")
			return
		}

        if age < 7 {
            fmt.Print("FREE")
        } else if age < 18 {
            fmt.Print("CHILD")
        } else {
            fmt.Print("ADULT")
        }

		// TODO: Реализуйте определение категории возраста по условиям задачи.
		// TODO: Используйте цепочку else if и выводите ровно одно слово: FREE, CHILD или ADULT.
		// В текущем шаблоне все валидные возраста считаются ADULT (это намеренно неполная логика).
		// fmt.Print("ADULT")
	}
}