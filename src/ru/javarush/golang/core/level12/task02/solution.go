package main

/*
= Задача №2. Окно по индексу =
Вы пишете мини-инструмент для просмотра “окна” данных в логе: есть фиксированный набор значений s := []int{10, 20, 30, 40, 50, 60}.
Оператор вводит позицию i (с какого индекса начать) и длину окна n (сколько элементов показать), чтобы увидеть ровно нужный кусок, не больше и не меньше.
Программа должна считать два целых числа i и n. Если параметры задают корректное окно из n элементов, начиная с индекса i (индексация 0-based), программа должна вывести под‑слайс s[i : i+n]. Здесь важно помнить полуинтервал: верхняя граница — это “позиция после последнего элемента”, поэтому именно i+n.
Если окно невозможно (например, i < 0, n < 0 или i+n > len(s)), программа должна вывести [] и завершиться без паники.
Требования:
• В программе должен быть использован ровно один заранее заданный слайс s := []int{10, 20, 30, 40, 50, 60} без его модификации.
• Программа должна считать два целых числа i и n через fmt.Scan(&i, &n).
• Перед взятием под-слайса программа должна проверить, что i >= 0, n >= 0 и i+n <= len(s); отдельный граничный случай: i == len(s) допускается только при n == 0.
• При корректных границах программа должна получить результат исключительно выражением s[i : i+n] (без append/copy и без создания “нового” слайса через копирование).
• При корректных границах (включая n == 0) программа должна вывести результат через fmt.Println (для n == 0 должен печататься пустой слайс).
• Если окно некорректно, программа должна вывести [] (fmt.Println([]int{})) и завершиться без panic при любом вводе.
*/

import "fmt"

func main() {
	// Фиксированный исходный слайс по условию (не модифицируем).
	s := []int{10, 20, 30, 40, 50, 60}

	var i, n int
	fmt.Scan(&i, &n)

	valid := false
	// TODO: Реализуйте проверку, что окно с параметрами i и n корректно для слайса s
	// TODO: При некорректных параметрах нужно вывести пустой слайс и завершиться без panic
	if i >= 0 && n >= 0 && (len(s) >= i + n) valid = true

	if !valid {
		fmt.Println([]int{})
		return
	}

	left, right := i, i + n
	// TODO: Вычислите границы окна (left/right) по введённым i и n
	// TODO: Сформируйте результат только через slicing исходного слайса s

	fmt.Println(s[left:right])
}