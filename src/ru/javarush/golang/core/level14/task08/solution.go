package main

/*
= Задача №8. Паспорт рун =
Вы собираете диагностический отчёт для строки из пользовательского ввода: сколько байт она занимает, сколько в ней Unicode‑символов, и где именно (в байтах) начинается каждый символ. Такой отчёт помогает понять, почему срезы «по индексам» иногда ведут себя неожиданно.
Программа читает одну строку без пробелов и печатает таблицу. Сначала одна строка-итог bytes=<len(s)> runes=<количество_рун>. Затем для каждой руны — отдельная строка k=<номер_руны_с_1> i=<байтовый_индекс> code=<код_руны> char=<символ>, где k — ваш счётчик 1,2,3…, а i берётся из for i, r := range s.
Требования:
• Программа должна прочитать одну строку без пробелов из stdin с помощью fmt.Scan.
• Программа должна вывести первой строкой ровно в формате `bytes=<len(s)> runes=<N>` с переводом строки, где `<len(s)>` — длина строки в байтах (len(s)), а `<N>` — количество Unicode-символов (рун), посчитанное через обход строки `for range`.
• Количество рун в строке должно быть получено именно через `for range` по строке (в отдельном проходе или совместив с печатью строк таблицы), без использования `utf8.RuneCountInString` и других пакетов.
• Для каждой руны программа должна вывести отдельную строку в формате `k=<номер_руны_с_1> i=<байтовый_индекс> code=<код_руны> char=<символ>`, где `i` берётся из переменной `i` в цикле `for i, r := range s`, а `r` — руна из этого же цикла.
• `k` должен быть отдельным счётчиком рун, начинающимся с 1 и увеличивающимся на 1 для каждой следующей руны; `i` должен быть байтовой позицией начала руны; строки отчёта должны выводиться в порядке обхода строки слева направо (стабильный вывод без перестановок).
*/

import "fmt"

func main() {
	var s string
	fmt.Scan(&s)

	// TODO: Посчитайте количество рун (Unicode-символов) через range по строке.
	// Важно: нельзя использовать utf8.RuneCountInString и нельзя импортировать другие пакеты.
	runes := 0
	for range s {
		// TODO: Увеличьте счётчик рун внутри цикла.
		runes++
	}

	fmt.Printf("bytes=%d runes=%d\n", len(s), runes)

	// TODO: Выведите построчный отчёт по каждой руне через `for i, r := range s`.
	// i должен быть байтовым индексом начала руны (значение i из range),
	// k — отдельным счётчиком рун, начиная с 1.
	k := 1
	for i, r := range s {
		// TODO: Напечатайте строку в точном формате:
		// k=<номер_руны_с_1> i=<байтовый_индекс> code=<код_руны> char=<символ>
		// code и char должны быть получены из текущей руны r.
		fmt.Printf("k=%d i=%d code=%d char=%c\n", k, i, r, r)
		k++
	}
}