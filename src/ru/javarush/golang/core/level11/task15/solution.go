package main

/*
= Задача №15. Потерянный append =
Вы обучаете стажёра: он уверен, что если вызвать append внутри функции, то «оно само добавится» в исходный слайс. Вы хотите показать, почему это ловушка, и как выглядит правильный вариант.
Напишите программу без ввода. Создайте слайс s со значениями 1 и 2. Затем вызовите функцию addWrong(s []int), внутри которой делается append(s, 999), но результат намеренно игнорируется и не возвращается.
После вызова addWrong выведите s через fmt.Println(s) — он должен остаться [1 2]. Затем вызовите addRight(s []int) []int, которая добавляет 999 правильно (с сохранением результата append и возвратом), присвойте результат обратно в s и выведите s ещё раз. Должны получиться ровно две строки вывода.
Требования:
• Программа не должна читать stdin и в main должна создать слайс s со значениями 1 и 2.
• В программе должна быть функция addWrong(s []int), внутри которой вызывается append(s, 999), но результат append намеренно не сохраняется и функция ничего не возвращает.
• После вызова addWrong(s) в main нужно вывести s через fmt.Println; в первой строке вывода должен быть ровно [1 2].
• В программе должна быть функция addRight(s []int) []int, которая делает append(s, 999), сохраняет результат и возвращает обновлённый слайс.
• В main результат addRight(s) должен быть присвоен обратно в переменную s (например, s = addRight(s)).
*/

import "fmt"

func main() {
	// Исходный слайс. Важно: программа без ввода.
	s := []int{1, 2}

	addWrong(s)
	fmt.Println(s) // должно остаться: [1 2]

	s = addRight(s)
	fmt.Println(s) // должно стать: [1 2 999]
}

// append возвращает новый (возможно) слайс-хедер, но здесь мы его "теряем" — это и есть ошибка.
func addWrong(s []int) {
	append(s, 999)
}

func addRight(s []int) []int {
	// TODO: Реализуйте корректное добавление 999 через append: сохраните результат append и верните обновлённый слайс.
	s = append(s, 999)
	return s
}