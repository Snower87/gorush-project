package main

/*
= Задача №5. Сигнал термостата =
Вы пишете логику для простого термостата в серверной: датчик присылает целое значение температуры, а термостат должен
вывести короткий статус — чтобы оператору было видно, "минус", "ровно ноль" или "плюс".
Программа читает одно целое число t и печатает ровно одно слово:
- FREEZE, если t < 0
- ZERO, если t == 0
- HEAT, если t > 0
Требования:
• Программа должна считать из stdin одно целое число t с помощью fmt.Scan.
• Логика определения статуса должна быть реализована цепочкой if / else if / else, а не отдельными if.
• В условиях веток должны использоваться сравнения t с 0 (t < 0, t == 0, t > 0), чтобы выражения в if/else if имели тип bool.
• У каждой ветки должны быть обязательные фигурные скобки, а else должен быть написан в стиле Go на одной строке с закрывающей скобкой предыдущей ветки: } else {.
*/

import "fmt"

func main() {
	var t int
	fmt.Scan(&t)

	// TODO: Реализуйте корректный вывод статуса по значению t (меньше / равно / больше нуля).
	// Используйте цепочку if / else if / else и сравнения t с 0.
	if t < 0 {
		// TODO: Выведите правильное слово для случая t < 0.
		fmt.Println("FREEZE")
	} else if t == 0 {
		// TODO: Выведите правильное слово для случая t == 0.
		fmt.Println("ZERO")
	} else {
		// TODO: Выведите правильное слово для случая t > 0.
		fmt.Println("HEAT")
	}
}