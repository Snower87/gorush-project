package main

/*
= Задача №9. Удалить без шума =
Вы пишете демонстрационный пример для стажёров: есть фиксированный список numbers, из него нужно “по-честному” удалить один элемент, сохранив порядок, и при этом правильно обработать память — очистить освободившийся слот.
В main создайте numbers := []int{5, 10, 15, 20, 25} и удалите элемент по индексу removeIndex := 2 (это число 15). Удаление оформите отдельной функцией removeAtStable(s []int, removeIndex int) []int, которая делает сдвиг через copy, уменьшает длину через reslice и очищает последний слот через clear.
После удаления выведите на первой строке итоговый слайс, а на второй строке — его len. Важно: так как длина меняется, main должен сохранить результат функции обратно в numbers.
numbers := []int{5, 10, 15, 20, 25}
removeIndex := 2
numbers = removeAtStable(numbers, removeIndex)
fmt.Println(numbers)
fmt.Println(len(numbers))
Требования:
• В программе должна быть реализована отдельная функция removeAtStable(s []int, removeIndex int) []int, выполняющая удаление элемента из слайса.
• Если removeIndex выходит за пределы допустимого диапазона индексов слайса, функция removeAtStable должна вернуть исходный слайс без изменений.
• При корректном removeIndex функция removeAtStable должна сохранять порядок элементов и выполнять сдвиг влево вызовом copy(s[removeIndex:], s[removeIndex+1:]).
• После сдвига функция должна уменьшить длину слайса через reslice и очистить освободившийся последний слот с помощью clear (последнего элемента исходного буфера перед reslice), затем вернуть результат как s[:len(s)-1].
• В main должен быть создан numbers := []int{5, 10, 15, 20, 25} и removeIndex := 2, а результат removeAtStable должен быть присвоен обратно переменной numbers с помощью оператора = (не через :=).
*/

import "fmt"

func removeAtStable(s []int, removeIndex int) []int {
	// Если индекс вне диапазона — возвращаем исходный слайс без изменений.
	if removeIndex < 0 || removeIndex >= len(s) {
		return s
	}

	// TODO: Реализуйте stable-удаление элемента из слайса, сохраняя порядок.
	// TODO: Сдвиньте элементы влево с помощью copy так, чтобы элемент с removeIndex исчез.
	// TODO: Очистите освободившийся последний слот через clear (до уменьшения длины).
	// TODO: Уменьшите длину через reslice и верните новый слайс.
	copy(s[removeIndex:], s[removeIndex+1:]) //копируем элементы влево
	//s[removeIndex] = 0
	clear(s[len(s)-1:])
	return s[:len(s)-1]
}

func main() {
	numbers := []int{5, 10, 15, 20, 25}
	removeIndex := 2

	// Важно сохранить результат обратно (не через :=), иначе легко ошибиться с использованием старого слайса.
	numbers = removeAtStable(numbers, removeIndex)

	fmt.Println(numbers)
	fmt.Println(len(numbers))
}