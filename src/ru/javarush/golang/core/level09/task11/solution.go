package main

/*
= Задача №11. Тихий парсер =
Вы делаете предобработку пользовательского ввода: в одном поле человек может написать что угодно, но вы хотите попробовать интерпретировать это как целое число и получить «мягкий» ответ — получилось или нет — без печати изнутри функции.
Программа читает одну строку-токен rawText и пытается распарсить её как int. Создайте функцию parseIntSafe(s string) (n int, ok bool): она возвращает ok == true, если парсинг успешен, и ok == false иначе. При ok == false значение n должно быть 0. Внутри используйте strconv.Atoi, а именованные результаты n и ok присваивайте через = (не через :=), чтобы случайно не создать новые переменные. В конце используйте return без значений.
Формат вывода:
* если ok == true: ok: <n>
* если ok == false: error
Требования:
• В программе должна быть реализована функция parseIntSafe(s string) (n int, ok bool) с именованными результатами n и ok.
• Функция parseIntSafe должна пытаться распарсить входную строку s в int с помощью strconv.Atoi.
• Если парсинг успешен, функция должна вернуть ok=true и n равным распарсенному числу; если парсинг неуспешен, функция должна вернуть ok=false и n=0.
• Внутри parseIntSafe присваивание значениям n и ok должно выполняться через оператор = (без использования :=), чтобы не создавать новые переменные и не затенять именованные результаты.
• Завершение функции parseIntSafe должно выполняться оператором return без указания возвращаемых значений (используются текущие значения именованных результатов n и ok).
*/

import (
	"fmt"
	"strconv"
)

func parseIntSafe(s string) (n int, ok bool) {
	// TODO: Реализуйте "мягкий" парсинг строки в int через strconv.Atoi.
	// TODO: При успехе верните (n=<число>, ok=true), при ошибке (n=0, ok=false).
	// TODO: Не используйте := при присваивании именованным результатам n и ok (только =).
	// TODO: В конце используйте return без значений.

	var err error

	// Вызов оставлен как часть каркаса, но результат пока не используется правильно.
	n, err = strconv.Atoi(s)
	//_ = err
	if err != nil {
	    ok = false
	    return
	}

	// Временное поведение (не соответствует требованиям полностью).
	ok = true
	return
}

func main() {
	var rawText string
	fmt.Scan(&rawText)

	n, ok := parseIntSafe(rawText)
	if ok {
		fmt.Printf("ok: %d", n)
	} else {
		fmt.Print("error")
	}
}