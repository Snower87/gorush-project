package main

/*
= Задача №1. Тёплое приветствие =
Вы пишете крошечный модуль для терминала в коворкинге: человек подходит, вводит своё имя одним словом, и система отвечает вежливо и одинаково каждый раз. Важно, чтобы формат приветствия был стабильным — это потом пригодится и для логов, и для тестов.
Программа должна прочитать из stdin одно слово — имя пользователя. Дальше вы строите приветствие строго в виде Hello, <name>!, но делаете это аккуратно через функции: одна функция только собирает строку, а другая — только печатает.
Требования:
• В программе должна быть реализована функция greet с сигнатурой greet(name string) string, которая формирует приветствие строго в формате Hello, <name>! и возвращает его как строку, не выполняя печать в stdout.
• В программе должна быть реализована функция printGreeting с сигнатурой printGreeting(name string), которая печатает ровно одну строку приветствия для переданного имени, используя результат greet.
• Программа должна прочитать из stdin одно слово (имя пользователя) с помощью fmt.Scan.
• Функция main должна содержать только чтение имени и вызов printGreeting(name) без дополнительной логики форматирования/сборки строки внутри main.
• Программа должна использовать только пакет fmt (без дополнительных импортов).
*/

import "fmt"

// greet собирает строку приветствия и ничего не печатает.
func greet(name string) string {
	// TODO: Реализуйте формирование строки приветствия в требуемом формате и верните её. Здесь не должно быть печати в stdout.
	return "Hello, " + name + "!"
}

// printGreeting печатает одну строку, используя результат greet.
func printGreeting(name string) {
	// TODO: Убедитесь, что печатается ровно одна строка приветствия, используя результат greet(name), без лишних пробелов/пустых строк.
	fmt.Println(greet(name))
}

func main() {
	var name string
	fmt.Scan(&name)
	printGreeting(name)
}