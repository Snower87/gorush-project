package main

/*
= Задача №1. Датчик ошибок =
Представьте, что вы собираете крошечный “датчик состояния” для консольного приложения: он хранит последнюю ошибку в переменной err. Пока ничего не произошло, ошибка должна быть “пустой” — то есть nil. Вам важно один раз увидеть это своими глазами, чтобы дальше не путаться: nil — это нормальный успешный исход, а не “что-то странное”.
Сделайте программу без ввода, которая объявляет переменную err типа error через var (без присваивания) и печатает три строки: правда ли, что err == nil, что именно печатается как значение err, и какой у err тип.
Требования:
• Программа не должна читать данные из stdin и должна работать без входных параметров.
• Программа должна объявить переменную строго как var err error, без какого-либо присваивания после объявления.
• Программа должна выполнить сравнение err с nil только выражением err == nil (без других способов проверки).
• Программа должна вывести ровно 3 строки, где первая начинается с err_is_nil=, вторая — с err_value=, третья — с err_type=.
• Третья строка (err_type=...) должна выводить тип переменной err с использованием fmt.Printf и плейсхолдера %T.
*/

import "fmt"

func main() {
	var err error

	// TODO: Выведите результат сравнения err == nil (только этим выражением).
	fmt.Printf("err_is_nil=%v\n", err == nil)

	// TODO: Выведите значение переменной err.
	fmt.Printf("err_value=%v\n", err)

	// TODO: Выведите тип переменной err через fmt.Printf и плейсхолдер %T.
	fmt.Printf("err_type=%T\n", err)
}