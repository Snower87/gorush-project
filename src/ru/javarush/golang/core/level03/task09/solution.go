package main

/*
= Задача №9. Зона дрона =
Вы пишете проверку для карты полёта: дрон присылает координаты точки, где он сейчас находится, а диспетчер хочет видеть
только простой ответ — находится ли дрон строго в “безопасной зоне №1”, то есть в первом квадранте.
Программа читает два целых числа x и y. Точка считается строго в первом квадранте, если одновременно x > 0 и y > 0.
Если это так — вывести "YES", иначе — вывести "NO".
Требования:
• Программа должна считать два целых числа x и y через fmt.Scan.
• Программа должна определить, что точка находится строго в первом квадранте только если одновременно выполняются условия x > 0 и y > 0.
• Условие принадлежности первому квадранту должно быть записано одним булевым выражением, использующим сравнения (>) и логический оператор &&.
• Программа должна использовать if/else и вывести ровно одну строку: YES при выполнении условия, иначе NO.
*/

import "fmt"

func main() {
	var x, y int
	fmt.Scan(&x, &y)

	// TODO: Реализуйте проверку: точка находится строго в первом квадранте только при выполнении условий из задания.
	if x > 0 && y > 0 {
		fmt.Println("YES")
	} else {
		fmt.Println("NO")
	}
}