package main

/*
= Задача №3. Расширяемая полка =
Представьте, что вы готовите “полку” под будущие данные: пока на ней нет ни одного элемента, но место заранее зарезервировано. В Go это выглядит так: s := make([]int, 0, 5) — длина 0, но ёмкость 5.
Программа должна считать одно целое число b — сколько ячеек вы хотите временно “открыть” для просмотра/записи прямо сейчас. Если 0 <= b <= cap(s), программа должна построить новый слайс t := s[:b] и вывести в трёх строках:
- сам слайс t (через fmt.Println),
- строку len=<значение len(t)>,
- строку cap=<значение cap(t)>.
Если b выходит за диапазон [0, cap(s)], программа должна вывести пустой слайс [] и строки len=0 и cap=0 — то есть “полку открыть нельзя”.
Требования:
• Программа должна создать слайс s только выражением make([]int, 0, 5), то есть с len(s)=0 и cap(s)=5.
• Программа должна считать одно целое число b из stdin с помощью fmt.Scan(&b).
• Программа должна проверить, что b находится в диапазоне 0 <= b <= cap(s), и выполнять нарезку s[:b] только после успешной проверки, чтобы исключить panic.
• Если b корректно, программа должна построить новый слайс t как t := s[:b] и не использовать индексирование элементов (s[i]) при len(s)==0.
• Если b корректно, программа должна вывести ровно 3 строки: (1) сам слайс t через fmt.Println, (2) строку вида len=<len(t)>, (3) строку вида cap=<cap(t)>.
• Если b некорректно (выходит за [0, cap(s)]), программа должна вывести ровно 3 строки: [] затем len=0 затем cap=0.
*/

import "fmt"

func main() {
	// По условию: исходный слайс должен быть создан только так: len=0, cap=5.
	s := make([]int, 0, 5)

	var b int
	fmt.Scan(&b)

	// TODO: Реализуйте проверку, что b находится в диапазоне [0, cap(s)] до выполнения нарезки, чтобы избежать panic.
	// TODO: Если b корректно — сформируйте слайс t нарезкой (без индексирования элементов) и выведите:
	// TODO: 1) сам t через fmt.Println, 2) len=<len(t)>, 3) cap=<cap(t)> (ровно 3 строки).
	// TODO: Если b некорректно — выведите ровно 3 строки: [] затем len=0 затем cap=0.
	if b >= 0 && b <= cap(s) {
	    t := s[:b]
        fmt.Println(t)
        fmt.Printf("len=%d\n", len(t))
        fmt.Printf("cap=%d\n", cap(t))
	} else {
    	// Базовый вывод-заглушка (сейчас программа всегда ведёт себя как при некорректном b).
    	fmt.Println([]int{})
    	fmt.Println("len=0")
    	fmt.Println("cap=0")
	}
}