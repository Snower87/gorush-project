package main

/*
= Задача №16. Скобки из файлов =
Вы собираете простую “упаковку” строки: слева должны появиться [[, справа — ]]. Команда хочет специально разнести левую и правую часть в разные файлы, чтобы потренироваться с несколькими init() внутри одного package main.
В проекте три файла: left.go, right.go и main.go. В left.go в init() готовится глобальная строка left = "[[", в right.go — right = "]]". В main.go читается одно слово s и выводится left + s + right.
Пример:
Ввод:
 go
Вывод:
 [[go]]
Требования:
*/

import "fmt"

func main() {
	var s string
	fmt.Scan(&s)

	// Собираем результат из заранее подготовленных частей.
	fmt.Print(left + s + right)
}