package main

/*
= Задача №14. Пошаговый тренажёр =
Вы пишете “тренажёр шагов” для новичка: он вводит число N, и программа печатает ровно N строк прогресса, чтобы человек видел, что цикл работает как надо. Каждая строка должна быть в формате step K, где K начинается с 1.
Если N <= 0, тренажёр не запускается — нужно вывести одну строку empty и завершиться.
Требования:
• Программа должна считать из stdin одно целое число N.
• Если N меньше либо равно 0, программа должна вывести ровно одну строку `empty` (с переводом строки) и завершиться без дополнительного вывода.
• Если N > 0, программа должна использовать цикл `for` (не `for {}`) и выполнить ровно N итераций.
• На каждой итерации программа должна печатать строку в формате `step K`, где K начинается с 1 и увеличивается на 1, заканчиваясь на N включительно.
• Каждая печатаемая строка (и `empty`, и строки `step K`) должна завершаться переводом строки.
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	if n <= 0 {
		fmt.Println("empty")
		return
	}

	// TODO: Реализуйте "тренажёр шагов": выведите ровно n строк в формате `step K`,
	// TODO: где K начинается с 1 и увеличивается на 1 до n включительно.
	// TODO: Используйте классический цикл `for init; cond; post` на ровно n итераций.
	for i := 1; i <= n; i++ {
	    fmt.Printf("step %d\n", i)
	}
}