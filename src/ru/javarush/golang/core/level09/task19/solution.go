package main

/*
= Задача №19. Шаговый счётчик =
Вы пишете симулятор «тикера» для мини-игры: есть стартовое значение, есть шаг, и при каждом тике нужно получать очередное число последовательности. Причём состояние должно храниться внутри функции-счётчика, чтобы main просто вызывал её много раз, не зная деталей.
Программа читает три целых числа: startValue, stepSize и ticksCount. Реализуйте makeStepper(start, step int) func() int, которая возвращает функцию-счётчик. Каждый вызов возвращённой функции должен выдавать очередное значение последовательности и увеличивать внутреннее состояние на step. Последовательность начинается со start.
В main создайте один счётчик и вызовите его k раз, печатая каждое полученное число с новой строки. Если k <= 0, программа не должна выводить чисел (разрешён вывод пустой строки или отсутствие вывода).
Требования:
• В программе должна быть реализована функция makeStepper(start, step int) func() int, возвращающая функцию без параметров, которая выдаёт числа последовательности.
• Возвращаемая из makeStepper функция при каждом вызове должна возвращать текущее значение, начиная со start, и затем увеличивать внутреннее состояние на step, чтобы следующий вызов вернул следующее число.
• Возвращаемая функция должна хранить “текущее значение” во внешней переменной (замыкание), а не получать/возвращать состояние через параметры.
• main должен прочитать три целых числа из stdin: startValue, stepSize и ticksCount (k).
• При k > 0 в main должен создаваться ровно один счётчик через makeStepper(startValue, stepSize), затем он должен вызываться ровно k раз, и каждое полученное значение должно печататься в отдельной строке.
• Если ticksCount (k) <= 0, программа не должна вызывать счётчик ни разу и не должна печатать числа (допускается отсутствие вывода или вывод пустой строки).
*/

import "fmt"

// makeStepper возвращает функцию-счётчик.
//
// ВАЖНО: состояние счётчика должно храниться внутри замыкания.
func makeStepper(start, step int) func() int {
	// TODO: Реализуйте хранение текущего значения внутри замыкания.
	// TODO: При каждом вызове возвращайте очередное значение, начиная со start,
	// TODO: и изменяйте внутреннее состояние на step для следующего вызова.

	current := start

	return func() int {
		// TODO: Верните текущее значение счётчика и обновите его для следующего тика.
		// Сначала отдаём текущее, потом сдвигаем состояние на шаг.
		v := current
		current += step
		return v
	}
}

func main() {
	var startValue, stepSize, ticksCount int
	fmt.Scan(&startValue, &stepSize, &ticksCount)

	if ticksCount <= 0 {
		return
	}

	next := makeStepper(startValue, stepSize)
	for i := 0; i < ticksCount; i++ {
		fmt.Println(next())
	}
}