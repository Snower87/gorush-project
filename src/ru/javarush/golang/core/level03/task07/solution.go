package main

/*
= Задача №7. Тариф по возрасту =
Вы автоматизируете выдачу категории билета: система получает возраст и флаг “студент/не студент”, а в ответ должна печатать ровно одну категорию. Важно: если возраст некорректный, всё остальное игнорируется — такие данные не должны проходить дальше.
Программа читает два целых числа: age и studentFlag (1 — студент, 0 — не студент).
Правила вывода:
- Если age меньше 0, вывести "INVALID AGE".
- Иначе, если age меньше 18, вывести "CHILD".
- Иначе (возраст 18 и больше): если studentFlag равен 1, вывести "ADULT DISCOUNT", иначе вывести "ADULT".
Требования:
• Программа должна прочитать из stdin два целых числа `age` и `studentFlag` с помощью `fmt.Scan`.
• Если `age < 0`, программа должна вывести ровно `INVALID AGE`, при этом значение `studentFlag` должно быть полностью проигнорировано (никакие другие ветки не выполняются).
• Если `age` неотрицательный и `age < 18`, программа должна вывести ровно `CHILD`.
• Если `age >= 18`, программа должна внутри этой ветки выполнить вложенную проверку `studentFlag`: при `studentFlag == 1` вывести `ADULT DISCOUNT`, иначе вывести `ADULT`.
• В логике ветвления должны использоваться только сравнения чисел (`==`, `<`, `>=`) без применения `&&`, `||`, `!`.
*/

import "fmt"

func main() {
	var age int
	var studentFlag int

	fmt.Scan(&age, &studentFlag)

	if age < 0 {
		fmt.Print("INVALID AGE")
		return
	} else if age < 18 {
	    fmt.Print("CHILD")
	} else {
	    if studentFlag == 1 {
	        fmt.Println("ADULT DISCOUNT")
	    } else {
	        fmt.Println("ADULT")
	    }
	}

	// TODO: Реализуйте выбор категории билета по правилам из условия.
	// TODO: Добавьте ветку для CHILD (возраст меньше 18).
	// TODO: Для возраста 18 и больше добавьте вложенный if по studentFlag (скидка для студента).
	// TODO: В ветвлениях используйте только сравнения (==, <, >=), без &&, ||, !.
	// fmt.Print("ADULT")
}