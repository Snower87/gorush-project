package main

/*
= Задача №17. Детектор невидимок =
Вы работаете в команде, которая ловит «призрачные баги» во вводе: лишние пробелы, табы, `\r\n` от Windows и другие невидимые символы. Обычный вывод строки ничего не показывает, поэтому вы делаете диагностический режим — чтобы увидеть строку как есть, её длину и реальные байты.
Напишите программу, которая читает из stdin ровно одну строку целиком (включая завершающий перевод строки) с помощью bufio.Reader и ReadString('\\n'). После чтения выведите «панель диагностики» ровно из трёх строк: строку в виде %q, длину в байтах и байты как []byte.
Такой вывод должен помогать сразу заметить, например, что пришло `\r\n` (в raw будет видно \\r\\n, а в байтах — числа 13 и 10).
Требования:
• Программа должна создать reader через bufio.NewReader(os.Stdin) и прочитать ровно одну строку целиком вызовом ReadString('\n'), чтобы считанная строка включала завершающий символ '\n' (если он присутствует во входе).
• После чтения программа должна вывести первую диагностическую строку в формате raw=%q, используя fmt.Printf (или эквивалентный вызов fmt с форматированием), чтобы «невидимые» символы отображались как escape-последовательности.
• Программа должна вывести вторую диагностическую строку с префиксом len= и числом, полученным как len(s) (длина в байтах, а не в рунах).
• Программа должна вывести третью диагностическую строку с префиксом bytes= и байтовым срезом []byte(s), напечатанным через формат %v.
*/

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	r := bufio.NewReader(os.Stdin)

	s := r.ReadString('\n')
	var err error

	// TODO: Считайте ровно одну строку из stdin через bufio.Reader так, чтобы строка включала завершающий '\n' (если он есть во входе).
	// TODO: Обработайте ошибку чтения без паники: при ошибке выведите понятное сообщение и завершите программу.

	if err != nil {
		fmt.Println("error:", err)
		return
	}

	fmt.Printf("raw=%q\n", s)
	fmt.Printf("len=%d\n", len(s))
	fmt.Printf("bytes=%v\n", []byte(s))
}