package main

/*
= Задача №13. Следующий пропуск: =
Вы делаете маленькую консольную утилиту для системы пропусков: оператор вводит id как текст (потому что иногда в поле по-
падают лишние символы), а программа пытается превратить это в число и показать, какой будет следующий номер.
Программа должна считать одну строку idStr, распарсить её через strconv.Atoi, получить id и err, затем вычислить
nextID = id + 1 и вывести три строки в точном формате:
- id = <число>
- nextID = <число>
- err = <ошибка или <nil>>
Требования:
• Программа должна считать ровно одно значение типа string (idStr) через fmt.Scan.
• Программа должна вызвать strconv.Atoi(idStr) и сохранить результаты в переменные id и err.
• Программа должна вычислить nextID как id + 1 (без дополнительных проверок).
• Программа должна вывести ровно три строки с помощью fmt.Println в точном формате, включая пробелы вокруг знака равенства: "id = <число>", "nextID = <число>", "err = <ошибка или <nil>>".
*/

import (
	"fmt"
	"strconv"
)

func main() {
	var idStr string
	fmt.Scan(&idStr)

	_ = strconv.IntSize // оставляем импорт strconv используемым

	var id int
	var err error

	// TODO: Преобразуйте строку idStr в число через strconv.Atoi и сохраните результаты в переменные id и err.
    id, err = strconv.Atoi(idStr)
	// TODO: Вычислите следующий идентификатор: nextID должен быть равен id + 1 (без дополнительных проверок).
	nextID := id + 1

	fmt.Println("id =", id)
	fmt.Println("nextID =", nextID)
	fmt.Println("err =", err)
}