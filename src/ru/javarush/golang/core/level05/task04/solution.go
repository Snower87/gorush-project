package main

/*
= Задача №4. Чётный режим =
У вас есть устройство, которое работает только на “чётных шагах” (0, 2, 4, ...). Оператор вводит верхнюю границу N, а программа должна вывести все чётные значения от 0 до N включительно — чтобы по этому списку можно было проверить, какие режимы доступны.
Если N = 0, нужно вывести только 0. Если N = 7, нужно вывести 0 2 4 6. Все числа печатаются в одной строке через пробел, без пробела после последнего числа, и с переводом строки в конце.
Требования:
• Программа должна прочитать одно целое число N из stdin с помощью fmt.Scan.
• Программа должна использовать цикл вида for init; cond; post со счётчиком, который инициализируется значением 0 и увеличивается на 2 на каждой итерации (например, i = i + 2).
• Программа должна вывести все чётные числа от 0 до N включительно в порядке возрастания; условие цикла должно допускать вывод N, если N — чётное.
• Все числа должны быть напечатаны в одной строке, разделённые ровно одним пробелом, без пробела после последнего числа, и с переводом строки в конце.
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	// TODO: Выведите все чётные числа от 0 до n включительно в одной строке:
	// TODO: - числа разделяются ровно одним пробелом
	// TODO: - после последнего числа пробела быть не должно
	// TODO: - в конце должен быть перевод строки
	// TODO: Запрещено использовать массивы или слайсы для накопления чисел перед выводом.
	for i := 0; i <= n; i = i + 2 {
        if i + 2 > n {
            fmt.Printf("%d\n", i)
        } else {
            fmt.Printf("%d ", i)
        }
	}
}