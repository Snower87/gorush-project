package main

/*
= Задача №10. Модуль координаты =
Вы отлаживаете простую систему координат в консольной игре: иногда координата уходит в минус (игрок левее нуля), но для некоторых расчётов нужен модуль — расстояние от нуля без знака. Важно, чтобы функция была написана через именованный результат и не оставляла его случайно равным нулю.
Программа читает целое число positionX и выводит его модуль. Реализуйте функцию abs(x int) (res int) с именованным результатом res. Используйте if и return без значений, но так, чтобы res получал корректное значение и при x < 0, и при x >= 0.
Пример ввода:
-7
Пример вывода:
7
Требования:
• В программе должна быть реализована функция abs(x int) (res int), где результат возвращается через именованную переменную res.
• Внутри функции abs должен использоваться return без указания возвращаемого значения (то есть «пустой return»), чтобы возвращалось текущее значение res.
• Функция abs должна с помощью if гарантированно присваивать res корректное значение и при x < 0 (res = -x), и при x >= 0 (res = x), чтобы модуль вычислялся верно.
• Для любых корректных входных данных функция abs не должна оставлять res в zero value из-за пропущенного присваивания (результат должен быть установлен явным присваиванием в нужной ветке).
• Функция main должна читать ровно одно целое число positionX из stdin с использованием fmt.Scan.
*/

import "fmt"

// abs возвращает модуль числа через именованный результат res.
// Важно: используйте if и "пустой return", но при этом res должен быть явно присвоен во всех ветках.
func abs(x int) (res int) {
	if x < 0 {
		res = -x
		return
	}
    res = x
	// TODO: Присвойте res корректное значение для случая x >= 0 (иначе res останется равным zero value).
	return
}

func main() {
	var positionX int
	fmt.Scan(&positionX)

	fmt.Println(abs(positionX))
}