package main

/*
= Задача №16. Разъехавшиеся слайсы =
Вы расследуете загадку в коде: два слайса выглядели как «одно и то же», но после роста одного из них они внезапно перестали делить общие данные. Вам нужно воспроизвести ситуацию, где append вынуждает слайс переехать на новый backing array.
Напишите программу без ввода, которая создаёт слайс s так, чтобы len(s)=0 и cap(s)=1 (через make). Затем добавьте в s число 1, сделайте «копию ярлыка» t := s, а потом добавьте в s число 2, чтобы ёмкости точно не хватило и произошла реаллокация.
После этого измените t[0] на 99 и выведите s и t в отдельных строках через fmt.Println. Ожидается, что s и t будут разными по содержимому, потому что после переезда они уже смотрят на разные массивы.
Требования:
• Программа должна создать слайс `s` выражением `make([]int, 0, 1)` без какого-либо ввода данных.
• Программа должна дважды добавить элементы в `s` строго в форме `s = append(s, value)`: сначала `1`, затем `2`, чтобы на втором добавлении гарантированно произошла реаллокация из-за нехватки `cap`.
• Программа должна выполнить присваивание `t := s` строго после первого `append` и строго до второго `append`.
• После второго `append` программа должна выполнить именно операцию `t[0] = 99`, чтобы изменение затронуло данные `t`, а не `s` (индекс 0 должен существовать).
*/

import "fmt"

func main() {
	s := make([]int, 0, 1)

	// TODO: Реализуйте сценарий, в котором после роста s происходит смена backing array, и изменения через t больше не влияют на s.
	s = append(s, 1)

	t := s
	// TODO: Добавьте необходимые операции со слайсами (включая append и изменение элемента) в правильном порядке.
	s = append(s, 2)
	t[0] = 99

	fmt.Println(s)
	fmt.Println(t)
}