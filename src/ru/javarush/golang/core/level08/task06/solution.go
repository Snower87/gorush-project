package main

/*
= Задача №6. Скорость датчика =
К вам приходит «сырой» показатель скорости от датчика: обычный int. Но внутри программы вы хотите хранить скорость в отдельном смысловом типе Speed, чтобы не перепутать её, например, с количеством предметов или уровнем игрока.
Программа читает rawSpeed, превращает его в Speed, а потом считает «скорость на следующей секунде», аккуратно добавляя шаг именно в типе Speed, а не «случайной единицей» типа int.
Требования:
• Программа должна прочитать одно целое число rawSpeed с помощью fmt.Scan.
• В программе должен быть объявлен пользовательский числовой тип type Speed int.
• После чтения rawSpeed программа должна явно преобразовать его в переменную s типа Speed через Speed(rawSpeed).
• Для прибавления 1 должна использоваться именованная константа Step типа Speed (например, const Step Speed = 1), а не литерал 1 типа int.
• Значение next должно вычисляться строго как s + Step.
*/

import "fmt"

// Speed — смысловой тип скорости, чтобы не путать с другими int.
type Speed int

// Step — именованный шаг скорости.
// TODO: Сделайте Step константой типа Speed и задайте шаг равным 1.
const Step Speed = 1

func main() {
	var rawSpeed int
	fmt.Scan(&rawSpeed)

	// TODO: Явно преобразуйте rawSpeed в Speed и сохраните в s.
	var s Speed = Speed(rawSpeed)

	// TODO: Посчитайте next как s + Step (в типе Speed).
	var next Speed = s + Step

	fmt.Printf("raw=%d type=%T\n", rawSpeed, rawSpeed)
	fmt.Printf("s=%d type=%T\n", s, s)
	fmt.Printf("next=%d\n", next)
}