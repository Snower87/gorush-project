package main

/*
= Задача №11. Честный максимум =
Вы пишете “умный” монитор показаний: приходит количество измерений N, затем сами значения (они могут быть отрицательными, например температура зимой). Нужно вывести максимальное значение.
Главная ловушка новичка — нельзя просто стартовать максимумом 0, потому что при всех отрицательных числах ответ будет неверным. Поэтому используйте две переменные, объявленные через var без явной инициализации:
max (int) — текущее максимальное значение,
hasMax (bool) — показывает, был ли max уже установлен данными.
Если N равно 0, программа должна вывести 0.
Требования:
• Программа должна читать из stdin сначала N (тип int), затем N целых значений, используя только fmt.Scan.
• Если введённое N равно 0, программа должна вывести 0 одной строкой и не пытаться читать значения измерений.
• Программа должна объявить две переменные без явной инициализации: max типа int и hasMax типа bool, и использовать их zero values (0 и false) для построения логики.
• При N > 0 программа должна корректно находить максимум даже если все измерения отрицательные (то есть нельзя полагаться на стартовое значение 0 как на максимум).
• Программа должна использовать цикл for, который выполняется ровно N раз, чтобы последовательно считать все N измерений и обновлять максимум.
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	if n == 0 {
		// По условию: ничего не читаем дальше
		fmt.Println(0)
		return
	}

	// По требованию: объявляем через var и используем zero values (0 и false)
	var max int
	var hasMax bool

	for i := 0; i < n; i++ {
		var x int
		fmt.Scan(&x)

		// TODO: Реализуйте корректное обновление максимума с учётом отрицательных чисел.
		// TODO: Используйте hasMax (zero value = false), чтобы на первом считанном значении
		// TODO: установить max, а на остальных — сравнивать с текущим max.
		if !hasMax {
		    max = x
			hasMax = true
		}
		if x > max {
			max = x
		}
	}

	fmt.Println(max)
}