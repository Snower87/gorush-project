package main

/*
= Задача №14. Хвост пароля =
Вы делаете простую проверку для системы логов: нужно вывести последний символ введённого “пароля” (одно слово без пробелов)
и его индекс, чтобы в отладке быстро видеть, чем заканчиваются строки. Самое опасное место — граница len(...), где легко случайно выйти за пределы.
Программа читает строку password (одно слово без пробелов) и выводит через пробел два значения: индекс последнего символа
и сам последний символ, например для go это 1 o. Если длина строки равна 0, программа должна вывести строку empty.
Индекс последнего символа нужно вычислять через длину строки, не выходя за границы, и в решении ожидается цикл, проходящий
по индексам строки от 0 до len(password)-1, чтобы в процессе получить последний индекс и последний символ.
Требования:
• Программа должна прочитать строку password (одно слово без пробелов) из stdin с помощью fmt.Scan.
• Если длина password равна 0, программа должна вывести ровно строку empty.
• Для непустой строки программа должна использовать цикл for с индексом, проходящий по индексам от 0 до len(password)-1 включительно, чтобы получить последний индекс и последний символ.
• Верхняя граница цикла должна быть задана так, чтобы не выйти за пределы строки, то есть использовать условие i < len(password) (а не i <= len(password)).
• Программа не должна обращаться к элементу password[len(password)] (и вообще не должна делать индексирование за пределами 0..len(password)-1).
*/

import "fmt"

func main() {
	var password string
	fmt.Scan(&password)

	if len(password) == 0 {
		fmt.Print("empty")
		return
	}

	var lastIndex int
	var lastChar byte

	lastIndex = -1
	lastChar = '?'

	for i := 0; i < len(password); i++ {
		// TODO: Реализуйте обновление lastIndex и lastChar в цикле так, чтобы после завершения цикла
		// TODO: они содержали индекс и символ, соответствующие последнему символу строки password (без выхода за границы).
		//_ = i
		lastIndex = i
		lastChar = password[i]
	}

	fmt.Printf("%d %c", lastIndex, lastChar)
}