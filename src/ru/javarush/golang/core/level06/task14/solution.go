package main

/*
= Задача №14. Проверка типов =
Вы настраиваете “диагностический режим” для учебного проекта: программа читает значения, а затем печатает и сами значения, и их типы. Это помогает быстро увидеть, как Go воспринимает введённые данные.
Программа должна считать из stdin два значения: целое число x и строку s (одно слово). Затем вывести две строки:
x=<значение> type=<тип>
s=<значение> type=<тип>
Для значения используйте %v, а для типа — %T. Важно: в каждой строке значение и тип должны относиться к одной и той же переменной (то есть переменная передаётся два раза: один раз под %v, второй — под %T).
Требования:
• Программа должна считать из stdin два значения: целое число x и строку s (одно слово), используя fmt.Scan или fmt.Fscan.
• Программа должна формировать весь вывод только с помощью fmt.Printf; использование fmt.Println (и аналогов для печати строк целиком) не допускается.
• Программа должна вывести строку формата `x=%v type=%T\n`, где в качестве аргументов под `%v` и под `%T` передаётся одна и та же переменная x (то есть x указывается два раза в вызове fmt.Printf).
• Программа должна вывести строку формата `s=%v type=%T\n`, где в качестве аргументов под `%v` и под `%T` передаётся одна и та же переменная s (то есть s указывается два раза в вызове fmt.Printf).
*/

import "fmt"

func main() {
	var x int
	var s string

	fmt.Scan(&x, &s)

	// TODO: Выведите значение x и его тип в требуемом формате через fmt.Printf (без fmt.Println и без лишнего текста).
	fmt.Printf("x=%v type=%T\n", x, x)

	// TODO: Выведите значение s и его тип в требуемом формате через fmt.Printf (без fmt.Println и без лишнего текста).
	fmt.Printf("s=%v type=%T\n", s, s)
}