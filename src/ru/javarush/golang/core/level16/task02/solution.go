package main

/*
= Задача №2. Своя тревога =
Вы пишете маленький терминал доступа: он принимает одно "кодовое слово". Если человек вводит ok, охранная система спокойна — ошибки нет (err == nil). Но если введено что-то другое, система должна поднять тревогу вашей собственной “фирменной” ошибкой, чтобы в логах сразу было видно: это не системная ошибка, а именно "плохой ввод".
Программа читает из stdin один токен. Для ok оставьте err равным nil. Для любого другого токена создайте значение собственного типа ошибки (который реализует интерфейс error) и сохраните его в переменную err типа error. Затем выведите, является ли err nil, как fmt печатает err, и какой у err тип.
Примерный шаблон вывода:
false
bad input: qwerty
main.BadInputError
Требования:
• Программа должна прочитать из стандартного ввода один токен (кодовое слово) для дальнейшей проверки.
• В программе должен быть объявлен собственный тип ошибки (например, badInputError), предназначенный для обозначения “плохого ввода”.
• Собственный тип ошибки должен иметь метод Error() string, возвращающий фиксированное сообщение (например, "bad input"), чтобы тип реализовывал интерфейс error.
• Должна быть переменная err типа error; если введено "ok", err должен остаться nil (без создания объекта ошибки), иначе в err должно быть записано значение собственного типа ошибки.
*/

import "fmt"

// error — это интерфейс, достаточно метода Error() string
type badInputError struct{}

func (badInputError) Error() string {
	// TODO: Верните фиксированное сообщение, чтобы badInputError реализовывал интерфейс error.
	return "bad input"
}

func main() {
	var code string
	fmt.Scan(&code)

	// Важно: err именно типа error.
	var err error

	// TODO: Реализуйте логику присваивания err:
	// TODO: - если введено "ok", err должен остаться nil (не создавайте значение ошибки)
	// TODO: - иначе err должен содержать значение вашего типа ошибки (badInputError)
	if code != "ok" {
	    var bie badInputError
	    err = bie
	}

	// 1) Результат сравнения с nil (строго через err == nil).
	fmt.Println(err == nil)

	// 2) Как fmt печатает err (печать err напрямую).
	fmt.Println(err)

	// 3) Фактический (конкретный) тип значения внутри интерфейса error.
	fmt.Printf("%T\n", err)
}