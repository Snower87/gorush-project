package main

/*
= Задача №5. Разгон процессора =
Вы пишете тест для маленького контроллера: он стартует с целого значения x и каждый “такт” удваивает его, пока значение
не станет достаточно большим для следующего режима. Вам нужно не только узнать финальное значение, но и понять, сколько раз контроллер успел “разогнаться”.
Программа читает одно целое число currentValue и многократно удваивает его, пока currentValue остаётся строго меньше 1000.
После завершения цикла программа должна вывести два числа через пробел: итоговое значение currentValue и количество выполненных удвоений doublingsCount. Если исходное значение уже не меньше 1000, итоговое значение равно исходному, а количество удвоений равно 0.
Требования:
• Программа должна прочитать из stdin одно целое число и сохранить его в переменную currentValue.
• Программа должна использовать цикл for с условием (без init/post), который выполняется, пока currentValue строго меньше 1000 и требуется продолжать удвоения.
• Внутри тела цикла программа должна изменять currentValue, удваивая его на каждом шаге.
• Программа должна вести отдельную целочисленную переменную doublingsCount и увеличивать её на 1 при каждом выполненном удвоении.
*/

import "fmt"

func main() {
	var currentValue int
	fmt.Scan(&currentValue)

    if currentValue <= 0 {
        return
    }

	doublingsCount := 0

	// TODO: Реализуйте цикл for в форме "как while" (без init/post).
	// TODO: В цикле удваивайте currentValue, пока он строго меньше 1000, и увеличивайте doublingsCount на 1 на каждом шаге.
	// TODO: Обеспечьте гарантированное завершение программы для любых целых входных данных (в том числе currentValue <= 0).
	for currentValue < 1000 && currentValue != 0 {
		currentValue *= 2
		doublingsCount++
	}

	fmt.Printf("%d %d", currentValue, doublingsCount)
}