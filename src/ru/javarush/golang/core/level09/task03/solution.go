package main

/*
= Задача №3. Лучший результат =
Вы делаете мини-табло для дружеского турнира: три игрока сыграли матч, и нужно честно показать лучший результат — одно число, без лишних объяснений. Чтобы код не превращался в "простыню" сравнений, вы решаете собирать максимум по шагам, как конструктор.
Программа читает три целых числа: firstScore, secondScore, thirdScore. Сначала реализуйте функцию max2(x, y int) int, которая выбирает большее из двух. Затем сделайте max3(a, b, c int) int, но так, чтобы внутри max3 максимум из трёх получался через вызовы max2, без одного огромного условия на три числа. Итоговый максимум печатается в stdout.
Требования:
• В коде должна быть реализована функция max2(x, y int) int, которая возвращает большее из двух целых чисел (при равенстве возвращает это же значение).
• В коде должна быть реализована функция max3(a, b, c int) int, которая находит максимум из трёх чисел, причём вычисление внутри max3 должно быть сделано через вызовы max2 (без одного большого условия, сравнивающего сразу три значения).
• Функции max2 и max3 не должны содержать fmt.Scan, а также не должны печатать в stdout/stderr.
• Функция main должна считать три целых числа (firstScore, secondScore, thirdScore) через fmt.Scan.
• Программа должна вывести в stdout ровно одно число — итоговый максимум из трёх введённых значений — без дополнительных сообщений.
*/

import "fmt"

func max2(x, y int) int {
	// TODO: Реализуйте функцию, которая возвращает большее из двух целых чисел (при равенстве верните это же значение).
	if y >= x {
	    return y
	}
	return x
}

func max3(a, b, c int) int {
	// TODO: Реализуйте максимум из трёх чисел через вызовы max2 (без одного большого условия, сравнивающего сразу три значения).
	// Временная логика: сравниваем только первые два значения, третье игнорируется.
	return max2(max2(a, b), max2(b, c))
}

func main() {
	var firstScore, secondScore, thirdScore int
	fmt.Scan(&firstScore, &secondScore, &thirdScore)

	fmt.Print(max3(firstScore, secondScore, thirdScore))
}