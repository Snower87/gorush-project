package main

/*
= Задача №20. Редактор команд =
Вы собираете мини-редактор списка слов: он получает стартовый список list, а затем выполняет команды, как в простейшем текстовом редакторе. Есть две операции: удалить диапазон и вставить слово. Ошибочные команды должны просто игнорироваться — редактор не имеет права падать.
Программа читает N, затем N слов (строк без пробелов) в слайс list. Затем читается число Q — количество операций. Далее идут Q операций двух видов:
Команда DEL i j должна удалить диапазон [i:j) из list.
Команда INS i x должна вставить слово x в позицию i.
Индексация везде с нуля. Для DEL операция считается корректной, если 0 <= i <= j <= len(list). Для INS операция корректна, если 0 <= i <= len(list). Некорректные операции должны быть проигнорированы без изменения list.
После выполнения всех операций программа должна вывести итоговый list: слова через пробел в одной строке.
Требования:
• Программа должна прочитать целое N, затем N слов (строк без пробелов) и сохранить их в слайс list. Затем программа должна прочитать целое Q и далее обработать ровно Q команд, считывая команду как строку через fmt.Scan.
• Для команды вида DEL i j программа должна удалить из list полуинтервал [i:j) только если диапазон корректен (0 <= i <= j <= len(list)); иначе команда должна быть проигнорирована без изменения list и без паники. Удаление должно выполняться через slices.Delete с обязательным присваиванием результата обратно: list = slices.Delete(list, i, j).
• Для команды вида INS i x программа должна вставить слово x в позицию i только если индекс корректен (0 <= i <= len(list)); иначе команда должна быть проигнорирована без изменения list и без паники. Вставка должна выполняться через slices.Insert с обязательным присваиванием результата обратно: list = slices.Insert(list, i, x).
• Программа не должна падать при некорректных индексах/диапазонах; любые некорректные операции DEL/INS должны не менять list. Команды обрабатываются как описано (двух видов), без дополнительных действий, влияющих на список.
• Внутри условий и циклов нельзя создавать новую переменную list через :=; должна изменяться и переиспользоваться исходная переменная list (в том числе при присваивании результата slices.Delete/Insert).
*/

import (
	"fmt"
	"slices"
)

func main() {
	var n int
	fmt.Scan(&n)

	list := make([]string, n)
	for i := 0; i < n; i++ {
		fmt.Scan(&list[i])
	}

	// Используем пакет slices, но ключевая логика редактирования списка вынесена в TODO ниже.
	list = slices.Clone(list)

	var q int
	fmt.Scan(&q)

	for k := 0; k < q; k++ {
		var cmd string
		fmt.Scan(&cmd)

		if cmd == "DEL" {
			var i, j int
			fmt.Scan(&i, &j)

			// TODO: Реализуйте удаление диапазона из list по команде DEL:
			// TODO: 1) проверьте корректность границ (чтобы не было panic),
			// TODO: 2) удалите элементы полуинтервала, обновив list результатом операции из пакета slices,
			// TODO: 3) некорректную команду полностью игнорируйте (list не меняется).
			if (0 <= i && i <= j && j <= len(list)) {
			    list = slices.Delete(list, i, j)
			}
		}

		if cmd == "INS" {
			var i int
			var x string
			fmt.Scan(&i, &x)

			// TODO: Реализуйте вставку слова x в позицию i по команде INS:
			// TODO: 1) проверьте корректность индекса (чтобы не было panic),
			// TODO: 2) вставьте элемент, обновив list результатом операции из пакета slices,
			// TODO: 3) некорректную команду полностью игнорируйте (list не меняется).
			// TODO: Важно: не создавайте новую переменную list через :=, переиспользуйте существующую.
			if (0 <= i && i <= len(list)) {
			    list = slices.Insert(list, i, x)
			}
		}

		// Неизвестная команда — игнорируем.
	}

	for i := 0; i < len(list); i++ {
		if i > 0 {
			fmt.Print(" ")
		}
		fmt.Print(list[i])
	}
	fmt.Println()
}