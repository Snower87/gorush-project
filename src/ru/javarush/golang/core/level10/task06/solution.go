package main

/*
= Задача №6. Константы протокола =
Вы пишете крошечный “плакат-памятку” для HTTP: иногда полезно быстро вывести метод и статус‑код, чтобы убедиться, что вы используете правильные константы из стандартной библиотеки.
Ввод не нужен. Программа должна вывести одну строку, составленную из http.MethodGet и http.StatusOK, разделённых пробелом: "GET 200".
Требования:
• Программа должна быть в пакете main и содержать функцию main() как единственную точку запуска.
• Программа должна импортировать ровно два пакета: fmt и net/http; никакие другие пакеты импортировать нельзя.
• В коде должны использоваться именно константы http.MethodGet и http.StatusOK (не строковый/числовой литерал и не свои константы).
• Программа должна вывести ровно одну строку вида "GET 200" (метод, пробел, статус-код) и завершить её переводом строки.
• Программа не должна читать данные из stdin (никаких fmt.Scan/Fscan/Reader/Scanner и т.п.).
*/

import (
	"fmt"
	"net/http"
)

func main() {
	// TODO: Сформируйте строку из http.MethodGet и http.StatusOK, разделив их пробелом, и выведите ровно одну строку с переводом строки.
	fmt.Printf("%s %d\n", http.MethodGet, http.StatusOK)
}