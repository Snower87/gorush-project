package main

/*
= Задача №5. Уровень героя =
Вы делаете прототип RPG, и у персонажа есть «уровень». Важно не перепутать его с обычным int: сегодня это просто число, а завтра вы захотите отдельные правила, проверки и функции именно для уровня.
Поэтому вы создаёте собственный тип Level на базе int, заводите переменную level со значением 3 и печатаете две версии: как Level и как обычный int после явного преобразования. Так вы глазами увидите, что типы разные.
Требования:
• Программа должна быть в package main и содержать функцию main как точку входа.
• В коде должен быть объявлен пользовательский тип на базе int: type Level int.
• В main должна быть создана переменная level типа Level (не int) со значением 3.
• Для получения “обычного” числа должно использоваться явное преобразование Level → int в виде int(level).
• Программа должна вывести ровно 2 строки в точном формате: level=<значение> type=<тип> asInt=<значение> type=<тип> где для печати типа используется %T, а для значения — %v или %d.
*/

import "fmt"

// Level — отдельный тип для "уровня", чтобы не путать с обычным int.
type Level int

func main() {
	level := Level(3)

	// TODO: Выведите первую строку в точном формате из условия:
	// TODO: level=<значение> type=<тип>
	fmt.Printf("level=%v type=%T\n", level, level)

	// TODO: Выполните явное преобразование Level -> int (через int(level))
	// TODO: и выведите вторую строку в точном формате из условия:
	// TODO: asInt=<значение> type=<тип>
	asInt := int(level)
	fmt.Printf("asInt=%v type=%T\n", asInt, asInt)
}