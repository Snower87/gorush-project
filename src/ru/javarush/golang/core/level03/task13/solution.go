package main

/*
= Задача №13. Контроль прохода =
Вы помогаете охране на входе в закрытую аудиторию. В систему вводят возраст человека и простой флаг, есть ли документ. Сначала нужно отсеять явно “сломанные” данные (например, возраст -5), а уже потом решать — пускать или нет.
Программа читает два целых числа: age и hasID. age — возраст, hasID — флаг документа (0 означает “нет”, 1 означает “есть”).
- Если age меньше 0 или больше 130, программа должна вывести invalid age и завершить работу.
- Если hasID не равен 0 и не равен 1, программа должна вывести invalid id flag и завершить работу.
- Если возраст меньше 18, программа должна вывести denied.
- Если возраст 18 или больше, но hasID равен 0, программа должна вывести denied.
- Во всех остальных случаях программа должна вывести allowed.
Требования:
• Программа должна прочитать два целых числа `age` и `hasID` через `fmt.Scan`.
• Если `age < 0` или `age > 130`, программа должна вывести `invalid age` и немедленно завершить `main()` через `return`.
• Если `hasID` не равен 0 и не равен 1, программа должна вывести `invalid id flag` и немедленно завершить `main()` через `return`.
• После прохождения валидации программа должна вывести `denied`, если `age < 18` или если `age >= 18` и при этом `hasID == 0`.
• Во всех остальных валидных случаях программа должна вывести `allowed` (эта “успешная” ветка должна находиться в конце `main()` и не быть глубоко вложенной).
*/

import "fmt"

func main() {
	var age, hasID int
	fmt.Scan(&age, &hasID)

	// TODO: Проверьте корректность возраста по условию задачи.
	// TODO: Если возраст некорректный — выведите "invalid age" и завершите программу через return.
	if age < 0 || age > 130 {
		fmt.Println("invalid age")
		return
	}

	// TODO: Проверьте корректность флага документа по условию задачи.
	// TODO: Если флаг некорректный — выведите "invalid id flag" и завершите программу через return.
	if hasID != 0 && hasID != 1 {
		fmt.Println("invalid id flag")
		return
	}

	// TODO: Реализуйте логику отказа во входе после успешной валидации.
	// TODO: Если нужно отказать — выведите "denied" и завершите программу через return.
	if age < 18 || (age >= 18 && hasID == 0) {
		fmt.Println("denied")
		return
	}

	fmt.Println("allowed")
}