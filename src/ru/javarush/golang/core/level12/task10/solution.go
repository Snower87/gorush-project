package main

/*
= Задача №10. Опасный апгрейд =
Вы делаете две одинаковые “заготовки меню” из строк: A B C D. В первом случае вы берёте обычный вид на первые два элемента и “улучшаете” его через append — и рискуете случайно переписать исходное меню. Во втором случае вы заранее ставите защиту: ограничиваете cap у вида, чтобы append был вынужден выделить новый буфер.
Программа должна создать два независимых слайса строк с одинаковым содержимым: A B C D.
Для первого слайса base1 программа должна взять под‑слайс view1 = base1[:2], затем сделать view1 = append(view1, "X"). После этого программа должна вывести base1.
Для второго слайса base2 программа должна взять под‑слайс view2 = base2[:2:2], затем сделать view2 = append(view2, "X"). После этого программа должна вывести base2.
Требования:
• Программа должна создать base1 и base2 как два отдельных литерала []string с одинаковым содержимым: "A", "B", "C", "D" (без использования копирования одного в другой).
• Для base1 программа должна создать view1 = base1[:2], затем выполнить view1 = append(view1, "X") с обязательным сохранением результата append обратно в переменную view1, после чего вывести base1.
• Для base2 программа должна создать view2 = base2[:2:2] (чтобы cap(view2) == len(view2)), затем выполнить view2 = append(view2, "X") с обязательным сохранением результата append обратно в переменную view2, после чего вывести base2.
• Программа должна вывести ровно 2 строки через fmt.Println: сначала base1, затем base2 (без дополнительного вывода).
• В решении не должны использоваться copy и пакет slices, не должны использоваться функции (кроме main) и ввод данных из stdin.
*/

import "fmt"

func main() {
	// Два независимых базовых слайса с одинаковым содержимым.
	base1 := []string{"A", "B", "C", "D"}
	base2 := []string{"A", "B", "C", "D"}

	var view1 []string
	// TODO: Возьмите под-слайс на первые 2 элемента из base1 (без ограничения capacity).
	view1 = base1[:2]
	// TODO: Добавьте строку "X" через append, сохранив результат обратно в view1.
	view1 = append(view1, "X")

	fmt.Println(base1)

	var view2 []string
	// TODO: Возьмите под-слайс на первые 2 элемента из base2, но с ограничением capacity (full slice expression).
	// TODO: Добавьте строку "X" через append, сохранив результат обратно в view2.
	view2 = base2[:2:2]
	view2 = append(view2, "X")

	fmt.Println(base2)
}