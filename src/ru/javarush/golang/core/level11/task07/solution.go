package main

/*
= Задача №7. Одна корзина =
Вы делаете прототип корзины покупок: две переменные в коде выглядят как две «корзины», но на самом деле указывают на один и тот же набор товаров. Очень важно увидеть это на примере, чтобы не словить сюрприз в реальном коде.
В программе без ввода создайте слайс a := []int{1, 2, 3}, затем сделайте присваивание b := a. После этого измените b[0] на 99 и выведите оба слайса в отдельных строках в формате "a=<...>" и "b=<...>", причём a печатается раньше b.
Затем выведите ещё одну строку, где покажите len и cap обоих слайсов в формате "a: len=<...> cap=<...>; b: len=<...> cap=<...>".
Требования:
• В программе без ввода должен быть создан слайс a со значениями []int{1, 2, 3}.
• Должно использоваться присваивание b := a, без ручного копирования элементов и без вызовов, создающих новый буфер (например, без make).
• После присваивания должен быть изменён ровно один элемент: b[0] = 99.
• Программа должна вывести два слайса в отдельных строках в формате a=<...> и b=<...>, при этом строка с a должна быть напечатана раньше строки с b.
• Программа должна вывести отдельную строку в формате a: len=<...> cap=<...>; b: len=<...> cap=<...>, используя функции len и cap для a и для b.
*/

import "fmt"

func main() {
	a := []int{1, 2, 3}

	// TODO: Сделайте так, чтобы b и a указывали на один и тот же набор элементов (без копирования).
	// Сейчас здесь создаётся отдельный слайс, и изменения через b не отражаются в a.
	//b := append([]int(nil), a...)
	b := a

	b[0] = 99

	fmt.Printf("a=%v\n", a)
	fmt.Printf("b=%v\n", b)

	// TODO: Выведите len и cap для a и b в точном формате из задания.
	fmt.Printf("a: len=%d cap=%d; b: len=%d cap=%d\n", len(a), cap(a), len(b), cap(b))
}