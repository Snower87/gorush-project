package main

/*
= Задача №20. Независимая копия =
Вы получаете список чисел (например, результаты измерений), и вам нужно вытащить первые n значений в отдельный, полностью независимый буфер. Дальше этот буфер будут “крутить” как угодно: менять первый элемент, дописывать новые — и это не должно влиять на оригинальные данные.
Программа читает данные из stdin.
Формат ввода:
- Первая строка: два целых числа m и n.
- Вторая строка: m целых чисел.
Нужно реализовать функцию firstNCopy(src []int, n int) []int. Она должна взять первые n элементов (или все элементы, если n больше len(src)), создать новый слайс нужной длины через make и скопировать данные через copy. Функция должна вернуть новый слайс, не разделяющий backing array с src.
В main после получения копии нужно изменить копию (если она не пустая): установить copySlice[0] = 999, затем сделать copySlice = append(copySlice, 777). После этого вывести исходный слайс и копию на отдельных строках в формате:
orig: [...]
copy: [...]
Требования:
• Программа должна прочитать из stdin два целых числа m и n, затем прочитать m целых чисел в слайс orig, используя fmt.Scan (или fmt.Fscan с os.Stdin).
• Программа должна содержать функцию firstNCopy(src []int, n int) []int, которая берёт первые n элементов src (если n > len(src), то берёт len(src); если n < 0, то трактует n как 0) и возвращает новый слайс соответствующей длины.
• В firstNCopy результат должен создаваться через make с длиной, равной количеству копируемых элементов, а заполнение выполняться через встроенную функцию copy; возвращаемый слайс не должен разделять backing array с src.
• В main после получения copySlice программа должна (если copySlice не пустой) установить copySlice[0] = 999, затем выполнить copySlice = append(copySlice, 777).
• После изменения copySlice исходный слайс orig должен остаться без изменений (включая случай, когда append расширяет copySlice).
*/

import (
	"bufio"
	"fmt"
	"os"
)

func firstNCopy(src []int, n int) []int {
	if n < 0 {
		n = 0
	}
	if n > len(src) {
		n = len(src)
	}

	// TODO: Реализуйте создание независимой копии первых n элементов src.
	// TODO: Результат должен иметь длину n и не должен разделять backing array с src.
	// TODO: Используйте выделение памяти под новый слайс и копирование данных.
	dst := make([]int, n, n)
	copy(dst, src)

	return dst
}

func main() {
	in := bufio.NewReader(os.Stdin)

	var m, n int
	fmt.Fscan(in, &m, &n)

	orig := make([]int, m)
	for i := 0; i < m; i++ {
		fmt.Fscan(in, &orig[i])
	}

	copySlice := firstNCopy(orig, n)

	// Мутации только копии: orig не должен меняться ни при записи, ни при append.
	if len(copySlice) > 0 {
		copySlice[0] = 999
		copySlice = append(copySlice, 777)
	}

	fmt.Printf("orig: %v\n", orig)
	fmt.Printf("copy: %v\n", copySlice)
}