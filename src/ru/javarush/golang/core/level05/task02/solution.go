package main

/*
= Задача №2. Обратный запуск =
Представьте, что у вас консольный "пульт запуска": оператор вводит число N, и система должна выдать обратный отсчёт в одной строке — чтобы его можно было скопировать в отчёт или сверить с чек-листом. Никаких лишних слов, только числа.
Программа читает целое N (N ≥ 1) и выводит числа от N до 1 в одну строку через пробел. После последнего числа пробела быть не должно, а в конце строки обязательно должен быть перевод строки.
Требования:
• Программа должна прочитать одно целое число N (N ≥ 1) из стандартного ввода, используя fmt.Scan.
• Программа должна использовать цикл вида for init; cond; post, где инициализация задаёт счётчик i = N, шаг цикла выполняет уменьшение i--, а условие завершает цикл после вывода числа 1 (то есть 1 включительно).
• Программа должна вывести последовательность чисел N, N-1, ..., 1 в указанном порядке, не пропуская ни одного значения.
• Программа должна выводить числа в одну строку так, чтобы между соседними числами был ровно один пробел, и после последнего числа (1) пробела не было.
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	// TODO: Реализуйте вывод чисел от n до 1 (включительно) в одну строку.
	// TODO: Используйте цикл for init; cond; post с обратным счётом (i = n; i--; условие до 1 включительно).
	// TODO: Обеспечьте ровно один пробел между числами и отсутствие пробела после последнего числа.
	// TODO: В конце вывода обязательно добавьте перевод строки.

    for i := n; i >= 1; i-- {
		if i > 1 { // между числами ровно один пробел, в начале пробела нет
			fmt.Printf("%d ", i)
		}
        if i == 1 {
            fmt.Printf("%d\n", i)
        }
		//fmt.Print(i)
    }
}