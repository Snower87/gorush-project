package main

/*
= Задача №13. Билетная очередь =
Вы пишете маленький "автомат талонов" в МФЦ: оператор вводит n, и автомат должен выдать ровно n талонов с номерами, начиная с нуля. Здесь легко ошибиться на границе и выдать лишний талон или недодать один — а очередь это не простит.
Программа читает целое число ticketsCount и выводит ровно ticketsCount строк. На i‑й строке (нумерация с нуля) программа печатает число i, то есть при ticketsCount = 5 должны появиться 0, 1, 2, 3, 4 — каждое на отдельной строке. Если ticketsCount меньше либо равно 0, программа должна вывести одну строку: empty.
Требования:
• Программа должна прочитать одно целое число `ticketsCount` из стандартного ввода с помощью `fmt.Scan`.
• Если `ticketsCount <= 0`, программа должна вывести ровно одну строку `empty` и не запускать цикл вывода талонов.
• Если `ticketsCount > 0`, программа должна использовать цикл `for` со счётчиком для генерации номеров талонов.
• При `ticketsCount > 0` программа должна вывести числа строго в диапазоне от `0` до `ticketsCount-1` включительно, без пропуска и без лишнего значения.
• При `ticketsCount > 0` программа должна напечатать ровно `ticketsCount` строк в stdout, по одному числу на каждой строке, в порядке возрастания начиная с 0.
*/

import "fmt"

func main() {
	var ticketsCount int
	fmt.Scan(&ticketsCount)

	// Пустая очередь: печатаем ровно одну строку и сразу завершаемся.
	if ticketsCount <= 0 {
		fmt.Println("empty")
		return
	}

	// TODO: Реализуйте вывод ровно ticketsCount талонов, начиная с 0 (каждый номер — с новой строки).
	// TODO: Проверьте границы цикла, чтобы не было лишнего значения и не было пропусков.
	for i := 0; i <= ticketsCount - 1; i++ {
		// TODO: Напечатайте номер талона для текущей итерации.
		fmt.Println(i)
	}
}