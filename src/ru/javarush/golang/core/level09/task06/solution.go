package main

/*
= Задача №6. Границы замера =
Вы настраиваете стенд с датчиками: три измерения пришли подряд, и нужен минимальный и максимальный показатель — для отчёта и контроля диапазона. При этом результаты удобно получать сразу парой значений из функции, без «лишних переносов» логики в main.
Программа читает три целых числа: measureA, measureB, measureC. Реализуйте функцию minMax3(a, b, c int) (int, int), которая возвращает сначала минимум, затем максимум. В main используйте множественное присваивание и выведите две строки "min=<...>" и "max=<...>".
Требования:
• Программа должна считать три целых числа measureA, measureB, measureC из stdin с помощью fmt.Scan.
• В программе должна быть реализована функция minMax3(a, b, c int) (int, int).
• Функция minMax3 должна возвращать сначала минимальное значение (min), затем максимальное (max); порядок результатов важен.
• Минимум и максимум должны вычисляться с использованием операторов сравнения (<, >) и ветвлений if/else; использование циклов, массивов или слайсов запрещено.
• В main результат вызова функции должен присваиваться двумя идентификаторами слева, например: mn, mx := minMax3(a, b, c).
*/

import "fmt"

// minMax3 возвращает сначала минимум, затем максимум из трёх чисел.
func minMax3(a, b, c int) (int, int) {
	// TODO: Реализуйте поиск минимума и максимума среди a, b, c.
	// TODO: Используйте только сравнения (<, >) и ветвления if/else (без циклов, массивов и слайсов).
	// TODO: Важно: сначала верните min, затем max (порядок результатов важен).
	min := a
	max := a

	if min > b {
	    min = b
	}
	if min > c {
	    min = c
	}
	if max < b {
	    max = b
	}
	if max < c {
	    max = c
	}

	// Временная заглушка: возвращает значения, которые не являются корректными для общего случая.
	return min, max
}

func main() {
	var measureA, measureB, measureC int
	fmt.Scan(&measureA, &measureB, &measureC)

	mn, mx := minMax3(measureA, measureB, measureC)

	fmt.Printf("min=%d\n", mn)
	fmt.Printf("max=%d\n", mx)
}