package main

import "fmt"

/*
= Задача №5. Две цифры =
Вы делаете печать "кода ячейки" на складе: менеджер вводит число от 0 до 99, а система должна отдельно показать десятки и единицы — так удобнее проверять маркировку глазами и не путаться.
Программа читает целое число cellCode (от 0 до 99). Реализуйте функцию split2Digits(n int) (int, int), которая возвращает сначала десятки, затем единицы. В main результат нужно принять множественным присваиванием и вывести оба числа в одной строке через пробел.
Требования:
• Программа должна считать одно целое число n (cellCode) через fmt.Scan.
• В программе должна быть реализована функция split2Digits(n int) (int, int), возвращающая два значения.
• Функция split2Digits должна вычислять десятки как n / 10, единицы как n % 10 и возвращать строго в порядке: десятки, затем единицы.
• В main результат вызова функции должен приниматься множественным присваиванием ровно в виде tens, ones := split2Digits(n).
• Программа должна вывести в stdout ровно два числа (десятки и единицы) в одной строке, разделённые одним пробелом, и завершить вывод переводом строки.
*/

// split2Digits возвращает десятки и единицы числа n (0..99).
func split2Digits(n int) (int, int) {
	// TODO: Реализуйте выделение десятков и единиц для числа 0..99 и верните их в порядке: десятки, затем единицы.
	return n / 10, n % 10
}

func main() {
	var n int
	fmt.Scan(&n)

	// Важно: множественное присваивание ровно в таком виде по требованию задачи.
	tens, ones := split2Digits(n)

	// Два числа в одной строке через пробел + перевод строки.
	fmt.Println(tens, ones)
}