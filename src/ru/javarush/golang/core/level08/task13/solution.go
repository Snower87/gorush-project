package main

/*
= Задача №13. Один фонарь =
Вы делаете «панель индикаторов» для устройства: каждый индикатор — это отдельный бит. Чтобы включить ровно один индикатор по номеру n, обычно строят маску вида 1 << n. Вам нужно написать программу, которая по номеру индикатора создаёт такую маску.
Программа читает n (от 0 до 15), строит маску (тип uint) и печатает её сразу в двух видах: десятичном и двоичном, причём двоичный вид должен быть шириной ровно 16 символов с ведущими нулями — как на настоящем диагностическом экране.
Требования:
• Программа должна прочитать целое число n (0..15) из стандартного ввода с помощью fmt.Scan.
• Программа должна вычислить маску включения ровно одного бита как uint(1) << uint(n) (или эквивалентным выражением со сдвигом).
• Результат (маска) должен храниться в переменной типа uint.
• Программа должна вывести значение маски в десятичном виде как первое значение в строке вывода.
• Программа должна вывести значение маски во двоичном виде шириной ровно 16 символов с ведущими нулями, используя формат %016b, как второе значение в строке вывода.
*/

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	mask := uint(1) << uint(n)
	// TODO: Вычислите маску включения ровно одного бита по номеру n (0..15) с помощью битового сдвига (тип результата должен быть uint).

	fmt.Printf("%d %016b", mask, mask)
}