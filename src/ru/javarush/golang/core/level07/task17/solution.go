package main

/*
= Задача №17. Витрина точности =
Вы делаете экран статистики для приложения и хотите красиво показать число пользователю: только 2 знака после точки. Но вы также хотите убедиться, что “красивый вывод” — это всего лишь маска, а само значение в памяти не меняется.
Программа без ввода вычисляет ratio как 2.0/3.0 (тип float64) и печатает его тремя строками. В первой строке число выводится “как на витрине” — двумя знаками после точки. Во второй и третьей строках — диагностически, с высокой точностью, чтобы сравнить значения и увидеть: переменная осталась той же самой.
Требования:
• Программа не должна читать данные из stdin; значение ratio должно вычисляться прямо в коде как 2.0/3.0.
• Переменная ratio должна быть объявлена с типом float64.
• Для печати всех строк программа должна использовать fmt.Printf и форматные строки (не fmt.Print/Println).
• Программа должна вывести ровно три строки, каждая заканчивается переводом строки \n, и каждая строка начинается с метки ровно как в условии: “Pretty:”, “Raw:”, “Still x:”.
• В строке “Pretty:” значение ratio должно печататься ровно с 2 знаками после точки (формат вида %.2f).
• В строках “Raw:” и “Still x:” значение ratio должно печататься ровно с 17 знаками после точки (формат вида %.17f), причём “Still x:” выводит то же значение переменной ratio (переменная не должна изменяться ради “красивого” вывода).
*/

import "fmt"

func main() {
	// TODO: Вычислите значение ratio прямо в коде как 2.0/3.0 (stdin читать нельзя).
	// TODO: Убедитесь, что переменная ratio имеет тип float64.
	var ratio float64 = 2.0 / 3.0

	// TODO: Выведите ratio с 2 знаками после точки, используя fmt.Printf и нужную форматную строку.
	fmt.Printf("Pretty: %.2f\n", ratio)

	// TODO: Выведите ratio с 17 знаками после точки, используя fmt.Printf и нужную форматную строку.
	fmt.Printf("Raw: %.17f\n", ratio)

	// TODO: Выведите то же значение переменной ratio (не изменяя её ради округления) с 17 знаками после точки.
	fmt.Printf("Still x: %.17f\n", ratio)
}